# Drift — current state

## Native completion · 7 September 2026

Task: `drift-v2-handover`. Repository: `bomkino/pitchdog-drift`. Working branch: `codex/drift-native-completion`. Governing contract: `DRIFT-NATIVE-COMPLETION-V3`, including the static/animated WebP addendum. Only Drift is a write target. The user's installed application is not an installation target.

**Reconciled source:** remote native head `a7bfb155f359d26f21fc7b4ce7b1bed2cde57f48`, tree `30efc27bdfaf1c3ce66818d429a71d799b03847c`; remote main `340b5f631c9147890bc775c86a71af315dd17929` remains hybrid. Existing older local worktrees and untracked contributor guidance are preserved. Native implementation is isolated in a new worktree on the existing candidate branch. Main requires a pull request and successful `verify`; protections remain unchanged.

**Existing proof:** packaged application run [34099529107](https://github.com/bomkino/pitchdog-drift/actions/runs/34099529107) passed at the reconciled native head, including real external XCUITest document decisions, immutable Save/reopen, independent-document export ownership, native preview, decoded looped WebM/WebP/MP4 output, wide transparent PNG/sequence, decoded AAC alignment and destination/original integrity. Directing core run [34099525544](https://github.com/bomkino/pitchdog-drift/actions/runs/34099525544) passed. These are candidate receipts, not main or release acceptance.

**Active correction — H10/H08:** integration run [34099527309](https://github.com/bomkino/pitchdog-drift/actions/runs/34099527309) failed compiling `FinalNativeBoundaryTests` because it used DriftCore's internal `CardPose` initializer through an ordinary import. The same access failure was reproduced locally against the actual compiled DriftCore module; `@testable import DriftCore` passes that typecheck. The test import is corrected without changing production API or assertions. Complete integration and packaged application checks remain required on the corrected source.

**Local evidence and limits:** physical Apple M2, 8 GB RAM, macOS 27.0 (26A5425a), Swift 6.4. Command Line Tools lack XCTest and the Metal compiler; local XCTest invocation failed with `unable to resolve module dependency: 'XCTest'`. This is an unavailable toolchain boundary, not a passed test. The existing hosted arm64 macOS/Xcode route will run full integration and external UI proof. Pinned codec build succeeded using a temporary path without spaces; upstream libvpx Makefiles reject a whitespace-containing dependency scratch path. Actual source and artifacts remain associated with this worktree.

**Next action:** run the corrected integration on hosted Mac, inspect its first new failure if any, and close the remaining relevant creative/media/resource/UI boundaries. Then replace obsolete active build/release/documentation paths, validate the safe installer and publisher, accept the combined native candidate, merge normally, and test/publish the exact-main installer.

**Release:** not published. Native candidate metadata currently defaults to 0.4.0; root package metadata and the empty historical 0.3.0 hybrid draft still require reconciliation. No native tag or installer is accepted. Missing physical M1 Pro, minimum macOS 13.3, display, sleep and assistive-technology observations remain explicitly untested. Known build, project-safety or output failures cannot be waived as hardware gaps.
