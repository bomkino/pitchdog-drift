# Drift — current state

## Native completion · 7 September 2026

Task: `drift-v2-handover`. Repository: `bomkino/pitchdog-drift`. Branch: `codex/drift-native-completion`. Contract: `DRIFT-NATIVE-COMPLETION-V3`, including animated/static WebP. Only Drift is a write target. Existing worktrees, untracked contributor guidance, original handover and installed application are preserved.

**Source and authority:** main remains `340b5f631c9147890bc775c86a71af315dd17929` until the native PR is accepted. Main requires a normal PR and strict successful `verify`; protections are unchanged. Publication is an explicit exact-main dispatch. No native release or installed upgrade is claimed here.

**Proven candidate:** `a06294681bc86180f135ff952e8f709916be2bd8` fixed test-only access to DriftCore's CardPose initializer. Hosted [core](https://github.com/bomkino/pitchdog-drift/actions/runs/34105604131), [integration](https://github.com/bomkino/pitchdog-drift/actions/runs/34105604087) and [archived application](https://github.com/bomkino/pitchdog-drift/actions/runs/34105604051) passed. The application journey exercised real native Save/Save As/Cancel/Don't Save/Revert/recovery, immutable document/export ownership, latest scrub and Metal preview, looped WebP/WebM/movie output, wide alpha PNG/sequence and decoded licensed AAC. Downloaded artifact signing verified.

**Physical observation and correction:** that artifact launches on an Apple M2 with 8 GB RAM, macOS 27.0 (26A5425a). File → Open failed for a synthetic native project because the older installed app registered its extension as `dog.pitch.pitched-project` (confirmed by Spotlight metadata). Native document routing now resolves its own extension before strict archive validation. `a5973c69fddadeb6c7233fabe3fda979160b1f42` passed hosted core/integration; its application build exposed an actor-isolation error in the added override. The override now retains AppKit's main-actor contract. Real reopening and Light/Dark screenshots are pending on the corrected artifact.

**Release consolidation:** active scripts, CI and documentation now describe the native runtime. Required `verify` includes source tests, complete native integration and the exact archived application/DMG journey. The application proof records build and code-directory identity, captures Light/Dark windows and verifies unchanged canvas pixels. The 4 GiB unique-original ZIP64 round-trip/peak-memory boundary is now required in native CI. Legacy hybrid/Linux workflows and the temporary write-enabled source relay are retired from active source; historical code and published history remain. Default version is 0.4.0, ad-hoc and unnotarized. The separate protected Developer ID/notary lane remains fail-closed.

**Local checks:** 90 source test files / 587 tests passed; native user-guide checks and 13 installer/publisher regressions passed. The installer uses disposable-directory tests for normal-Quit cancellation, staged/post-copy failure, rollback, symlink targets, locks and interrupted replacement. Publisher tests cover matching draft/public retries, immutable tags/assets, exact main and missing/corrupt checksum/receipt identity. YAML parses and shell syntax checks pass. These tests do not substitute for the final mounted real bundle and public download.

**Local toolchain limits:** Command Line Tools provide Swift 6.4 but lack XCTest, Metal compiler and SwiftUI macro plugins. Local full tests/build cannot be claimed. Pinned native libraries build successfully with dependency scratch on a path without whitespace. Hosted full-Xcode arm64 checks supply the application build/UI route. Physical minimum macOS 13.3, M1 Pro, sleep/wake, battery, external display and assistive-technology acceptance remain untested. Known code, file-safety and output failures block release.

**Next:** pass the corrected combined native candidate, inspect real appearance/output artifacts and local reopening, merge through the protected PR, rebuild/test the exact main source, accept and publish its frozen installer, then redownload and mount the public bytes. Do not replace the user's installed app.

**Combined gate findings:** run `34109404212` at `d23e0f56761ed9f06e29f7ce7ef92df6b11ece36` passed all 587 source tests. The native capacity test wrote/reopened a 4,294,979,378-byte ZIP64 archive and verified all eight 512 MiB originals, but peak RSS growth was 610,418,688 bytes and failed the unchanged 512 MiB ceiling. FileHandle/Data streaming loops now drain Foundation autorelease buffers per chunk; the full capacity gate must pass on corrected source. The real application reached its Light window; XCUITest could read application-owned state but macOS rejected its screenshot write into another app's Application Support. Captures and acknowledgements now use the driver's own exchange directory. No permission or privacy protection is bypassed. Preview-audio acceptance now exercises the actual AVAudioEngine start/pause/resume/seek path and requires no rebuild across Look edits or stale restart after stop. Native source builds locally; full application proof still requires hosted Xcode.

**Local streaming proof:** a command-line diagnostic linked to the actual rebuilt DriftCore/DriftNative modules passed on the physical M2: archive 4,294,979,346 bytes, eight original hashes verified, 16,646,144-byte peak RSS, zero measured peak growth across Save/read/verify, 17.88 seconds. This is direct native storage evidence, not a substitute for XCTest or application UI acceptance. Diagnostic output is retained in the existing ignored build evidence directory.

**Protected PR proof:** [PR 49](https://github.com/bomkino/pitchdog-drift/pull/49), run `34110556149`, passed all 35 native integration tests, including the full 4 GiB ZIP64 boundary with zero measured peak RSS growth. Source checks passed. macOS also refused the UI driver's exchange-directory write; the driver now uses supported XCTest screenshot attachments and the actual Next frame button to acknowledge each capture. The application verifies unchanged artwork before that intentional frame step and restores its position afterward. Complete application/audio/installer acceptance remains pending.

**Rendered acceptance found and repaired a real defect:** asymmetric source quadrants rendered vertically mirrored although loop equality/alpha/dimensions passed. Core Image now normalizes source texture row orientation once at upload, preserving top-left crop coordinates. On the physical M2, the actual renderer now passes upright quadrants and top-half crop assertions. Controlled material/lighting/optics/atmosphere/path/performance comparisons change pixels; protected Pin remains identical. All 72 World frames are distinct across three repeated passes; peak RSS remains 54,804,480 bytes with zero measured repeat growth. These diagnostics use the rebuilt native modules and unchanged shader resources from `a0629468`; the complete final bundle still needs acceptance. The archived application gate now checks asymmetric orientation and crop pixels explicitly.

**Historical release:** empty draft release `383255443`, `v0.3.0`, is marked superseded and remains unpublished with no assets. Its tag/target metadata was read back. Publisher promotion now supplies the complete intended release metadata, avoiding omitted-field defaults; public assets and historical tags remain immutable.

**Remaining application gate:** run `34111654523` captured both Light/Dark windows and advanced through all silent movie/PNG outputs. The application then exited during the new live-audio case before producing its result; it was a process exit, not established evidence of a hung engine. The actual native audio module passed on the physical M2 (one soundtrack build, three starts across pause/resume/seek, no restart after Stop). XCUITest now detects premature exit immediately and collects the current app crash report/audio-device state; the audio proof records its last phase. Investigate the hosted crash before accepting a bundle. The consumer audit also repaired imported cover defaults mapping to Fit and removed controls for historical catalog-only fields while preserving serialized data and implemented recipe behavior.

**Audio crash root cause:** run `34113357421` produced the exact stack: `AVAudioPlayerNodeImpl.CompletionHandlerQueue` → `PreviewSound.enqueue` callback → `_dispatch_assert_queue_fail`. The callback inherited MainActor isolation under the older hosted SDK; the local newer SDK did not reproduce it. Explicit `@Sendable` now keeps the callback nonisolated before its deliberate MainActor hop. The hosted audio device is an Apple Virtual Sound Device with 48 kHz stereo output. No hardware waiver or substituted mixer is used. The import regression now exercises the actual asynchronous import path instead of constructing a batch without import authority.

**Additional physical/maintenance evidence:** real renderer pixels change during a fixed Spotlight pose (center mean byte difference 22.53); Closing-only has visible first/terminal frames across 23 output frames. PR #42's exact Mediabunny/AAC 1.55.5 patch updates are integrated into the retained build/reference inputs and pass all 587 source / 13 release tests. PR #43's grouped TypeScript 7 compiler migration is unrelated to native runtime delivery and remains open for separate consideration; no branch is deleted merely to reduce the count.

**Current boundary:** `a40cc0289914f40334ce0983d20eff0f359f2497` passes all 36 native integration tests. Its app run `34114131321` stopped earlier with `Video.mp4: Cannot Decode` during the same synthetic import that passed previous candidates; this is not an audio-fix result. The next complete candidate must re-establish media and live-audio acceptance. Captures also showed the 1360-wide default window extending past the hosted 1024-wide display. Initial/restored frames now fit the actual visible screen with a 960-point minimum; app/UI assertions require the complete window and Export control to be on screen. Physical UI work is awaiting the user's Mac unlock; no installed app is replaced.
