# Drift — current state

## Native completion · 7 September 2026

Task: `drift-native-completion`. Repository: `bomkino/pitchdog-drift`. Branch: `codex/drift-native-completion`; [PR 49](https://github.com/bomkino/pitchdog-drift/pull/49). Contract: `DRIFT-NATIVE-COMPLETION-V3`, including animated/static WebP. Only Drift is a write target. Original handover, existing worktrees, untracked contributor guidance and the installed application are preserved.

**Authority and release boundary:** main remains `340b5f631c9147890bc775c86a71af315dd17929`. Normal protected PR and strict `verify` are required; protections are unchanged. Publication requires a successful exact-main build and explicit dispatch. No native public release or installed upgrade is claimed. Intended version 0.4.0 is ad-hoc and unnotarized; Developer ID/notary credentials remain a separate protected lane.

**Current application proof:** [run 34114815984](https://github.com/bomkino/pitchdog-drift/actions/runs/34114815984), PR head `5481623c27d5367a657ca73ec5d4d7d078be42a0`, tested synthetic merge source `3f1efc727f6f7c6670418252dd3dc6a9b039ac84`. All 17 real application assertions and frozen/mounted DMG verification passed. Code-directory hash `958e989e624ff2006a5922995b441d78992140e1`; DMG SHA-256 `3663826a30cdb641a43adddb2e08b99f15bc9d35a21f7eaf7b4a745bf5924875`. Downloaded receipts and Light/Dark captures are inspected in `build/accepted-5481623`. Both appearances preserve artwork and now fit the hosted 1024-point display with Export visible.

**Covered real application behavior:** NSDocument Save/Save As/Cancel/Don't Save/Revert/private recovery; immutable in-flight Save and export ownership across independent documents; original hashes; latest source/preview scrub; upright asymmetric originals and top-half crop; Spotlight/Closing frames; animated WebP, VP8/VP9 WebM and MP4 Pin loop output; wide alpha PNG/sequence; licensed non-silent, finite, unclipped AAC with rational timing; cancellation preserving destinations. Live AVAudioEngine passed eight Look edits with one soundtrack build and three starts across pause/resume/seek, without stale restart after Stop.

**Combined gate:** [run 34116056756](https://github.com/bomkino/pitchdog-drift/actions/runs/34116056756) at `fceaeb3548807795c0ccdd186fb63d8246a38ed4` passes source (587 tests), installer/publisher (13 tests), all 36 native integration tests, the actual archived application and frozen DMG, and strict `verify`. The clock-boundary test now checks complete authored identity separately from commit time. The targeted real-control addition at `daf7ebea29759e48d489f20848322b411ec11611` passed Enter, then failed selection/blur acceptance in run `34116735780`. Numeric fields now retain the focused draft until its captured original-target commit finishes, even when selection-value and focus notifications arrive together. The unchanged UI regression and all-World window preview passes must succeed on this correction before acceptance.

**Repaired acceptance failures:** extension routing now handles coexistence with the old installed project UTI; per-chunk autorelease pools bound ZIP64 streaming memory; Metal source upload normalizes vertical orientation; imported cover/contain defaults map to Fill/Fit; controls for historical catalog-only fields are hidden while serialized values remain intact; the older SDK's inherited MainActor audio completion crash is fixed with an explicit Sendable callback; document windows are constrained to the actual display. XCTest captures use supported attachments and a real Next frame acknowledgement, preserving OS access boundaries.

**Physical M2 evidence:** Apple M2, 8 GB, macOS 27.0 (26A5425a). Earlier archived candidate launched. Actual rebuilt native modules wrote/reopened a 4,294,979,346-byte ZIP64 project with eight verified originals, 16,646,144-byte peak RSS and zero measured growth. Actual renderer comparisons distinguish material, lighting, optics, atmosphere, motion path and performance; protected Pin is pixel-identical. All 72 World frames are distinct over three repeated passes, peak RSS 54,804,480 bytes with zero measured repeat growth. Actual source pixels advance during a frozen Spotlight pose; Closing-only has visible first/terminal frames. These synthetic diagnostics do not establish representative full-app stress acceptance.

**Installer and publication:** archived app ZIP round-trip preserves signing and file bytes. Frozen DMG uses the accepted bundle without rebuild/re-sign. The stock-macOS installer validates source/version/build/architecture/code hash and exact receipt hashes, stages before normal Quit, preserves cancellation and rollback, and rejects unsafe targets. Thirteen regressions plus a real signed bundle in disposable Applications storage cover failure/retry boundaries. The downloaded candidate passes actual readonly-DMG fresh and replacement installation in disposable Applications storage; receipt hashes, code signature, rollback byte equality and removed install lock are read back in `build/native-integration/dmg-install-NCLvf3xm/RESULT.json`. Exact-main and public-download checks remain pending. Never replace the user's installed app under this task.

**Repository maintenance:** empty `v0.3.0` draft release `383255443` is marked superseded, retained unpublished with no assets. PR 42's exact Mediabunny/AAC 1.55.5 patch is integrated; retire that duplicate after acceptance. PR 43's grouped TypeScript 7 compiler migration remains open as separate work. Historical hybrid code/docs and published tags remain preserved; active runtime, CI, release scripts and docs now describe the native application.

**Limits and next action:** the user's Mac is locked. Remaining focused numeric editing, Look A/B/Apply/Cancel/Undo and repeated native window rendering are now covered by a targeted hosted UI acceptance addition; do not claim them passed until that run succeeds. Corrected physical File → Open remains untested. Local Command Line Tools lack XCTest, Metal compiler and SwiftUI macro plugins; full builds use hosted Xcode. Physical macOS 13.3, M1 Pro, battery, sleep/wake, external display and assistive-technology acceptance remain untested. Next: pass the corrected candidate, complete physical checks, merge normally, build/test exact main, publish its frozen installer, and independently verify public bytes. Known code, file-safety or output failures block release.
