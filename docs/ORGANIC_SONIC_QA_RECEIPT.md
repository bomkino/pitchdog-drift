# Organic editorial sonic-language QA receipt

This receipt covers the stacked draft branch
`feat/organic-editorial-sonic-language-gpt56pro`, proposed against the existing
tactile-sound branch. It grants no authority to merge, auto-merge, deploy, or
mark either pull request ready for review.

## Product claim under test

Drift turns one semantic edit into a restrained physical micro-gesture built
from the existing 23 pinned Kenney CC0 recordings:

- **body** identifies the material action;
- **air** describes motion through space;
- **contact** punctuates the edit;
- **landing** adds selective physical consequence.

The branch adds no music, generated sound, new binary corpus, dependency,
schema version, runtime host, ambience loop, hover sound, or slider chatter.

## Static and acoustic receipt

Local exact-source validation completed with:

- TypeScript project build: passing;
- unit and contract tests: **98/98 passing**;
- production build: passing;
- production bundle gate: **23 one-to-one hashed WAV assets**, 1,664,650 bytes,
  no inlined audio;
- acoustic treatment gate: **3.1 dB active-event spread**, **44 ms maximum
  onset**;
- pinned upstream provenance and CC0 verification: **23/23 passing**;
- `git diff --check`: passing.

The new unit gauntlet proves deterministic layer plans, stable bodies across
Texture changes, monotonic layer addition, sparse defaults, high-cadence
subtraction, role-specific filters and envelopes, bounded micro-timing,
secondary-gain budgets, centred non-spatial gestures, complete dependency
preloading, fatigue-resistant take rotation, palette-independent rhythm,
speed-responsive duration, shared envelope fitting, same-gesture physical-source
de-duplication, and one decode per physical source within each audio context.

## Browser receipt

Using the pinned Chromium artifact from the draft branch:

- deterministic organic PCM render across Editorial, Cinema, and Paper,
  bounded PCM, palette separation, sound-panel geometry, and loading recovery:
  **5/5 passing in one combined worker run**;
- trusted-intent lazy loading, same-origin payloads, mute persistence, first-click
  re-arm, and retry after one failed local WAV: passing;
- transient status notices no longer intercept the sound controls underneath;
  their explicit dismiss button remains interactive.

The local container exposes Drift's DOM fallback instead of a true WebGL stage,
so the drag/wheel interaction journey cannot be honestly re-certified here.
The local browser also rejects its own MP4 at the existing SDR Rec.709 gate
before mixed-audio assertions run. Neither gate was weakened. The authoritative
stacked-PR GitHub workflow must therefore re-run the full WebGL, interaction,
MP4, AAC, presenter-gap, cancellation, and recovery suite on the final commit.

## Listening artefact

A 17.5-second local reference reel was rendered from the same deterministic
export path, separated only by silence:

1. Editorial at 0.00 s;
2. Cinema at 6.05 s;
3. Paper at 12.10 s.

It is a review convenience, not a committed project asset and not a substitute
for the automated PCM and AAC gates.

## Final branch gate

Before this stacked draft is considered complete:

1. remove the temporary workspace workflow;
2. confirm only sound-language, tests, and documentation differ from the base;
3. run ordinary pull-request CI on the final head;
4. inspect all failures rather than rerunning blindly;
5. update the PR body with exact final receipts;
6. leave the pull request draft and unmerged.
