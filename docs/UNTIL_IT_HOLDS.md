# Until It Holds — run receipt

- Started: 20 August 2026, Asia/Kolkata
- Total-attempt cap: 5
- Outcome: A controllable cinematic pitch-slide production tool whose real captures survive technical and creative scrutiny.
- Artifact: this repository
- Authority: local reversible work, read-only research, and final public source-repository publication are allowed. No website deployment, binary release, email, purchase, or source deletion.
- Baseline: empty, non-git folder.

The frozen product bar is in `docs/PRODUCT_CONTRACT.md`. A critic verdict is recorded as evidence, never used as a substitute for inspecting the artifact.

## Rounds

### 1 — research and product contract: holds

- Builder: froze the real product as an Instagram-master tool, not a carousel demo; separated preview, deterministic output, alpha, persistence, presenter decode, and fallback promises.
- Critic: reference researcher plus technical-product architect.
- Initial break: contract blurred moving-track video scope, universal MP4 support, alpha output, reduced-motion export, and portable recovery.
- Repair: image-only moving track, one optional presenter decoder, current-Chrome export target, honest PNG alpha, fixed 1080 × 1920 master defaults, fresh-profile bundle proof, and visible failure boundaries.
- Evidence: primary reference study and the frozen contract.
- Decision: borrow principles and mechanisms, never compositions.

### 2 — renderer and motion engine: holds

- Builder: pure time evaluator, Three.js/WebGL2 scene, raw GLSL slides/backgrounds, five spatial flows, bounded optical distortion, continuous corners, pinned frame, and GPU limit preflight.
- Critic: hostile shader/engine pixel review in real Chrome.
- Initial breaks: linear-dark custom shaders, inverted contain math, false focal movement, stale transparency after themes, double-premultiplied transparent RGB, and inverted vertical focal semantics.
- Repair: Three color-space output chunks, aspect-derived cover/contain/focal math, top-left UI mapping, straight-alpha capture, explicit theme transparency, and preflight before canvas mutation.
- Evidence: exact mid-gray and half-alpha pixels, source-edge focal probes in both axes, 24 compiled shader paths, BT.709 MP4 samples, and unchanged renderer state after oversize rejection.
- Regressions: none in targeted recheck.

### 3 — deterministic export and durable projects: holds

- Builder: fixed-step MP4/PNG exporters, presenter sample decode, software AAC path, container readback, rollback-aware disk targets, IndexedDB storage, and hashed `.pitched` bundles.
- Critics: independent export and persistence adversaries.
- Initial breaks: cancellation after finalisation, wrong A/V interval checks, absent MP4 readback, timezone-unstable ZIP metadata, weak directory cleanup, AAC priming tail, and presenter freeze outside its source duration.
- Repair: cancellation-aware targets, decoded container verification, strict audio coverage/tail gates, capped 24/25/30 fps audio masters, exact PNG sequence checks, stable archive metadata, and strict presenter sample availability.
- Evidence: 21 export tests, eight persistence tests, real-Chrome streaming/cancel/alpha/AAC probes, exact 240-frame default proof, and fresh-profile project reopen.
- Regressions: none in module rechecks.

### 4 — controls, browser fallback, and accessibility: holds

- Builder: authored six-theme director, responsive three-panel shell, keyboard reorder, visible focus, honest file inputs, 24/25/30/50/60 fps controls, and DOM fallback.
- Critics: fresh responsive/accessibility and visual reviewers.
- Initial breaks: theme transparency leaked, mobile radio inputs expanded the page, focus was invisible, file inputs were tabbable offscreen, reorder was drag-only, and fallback MP4 opened a destination picker before failing.
- Repair: coherent theme reset, contained radios, focus-visible treatment, hidden non-Tab inputs with visible triggers, keyboard up/down controls, exact mobile containment, and renderer preflight before any picker.
- Evidence: 320/390 px viewport equality, stable footer, picker instrumentation, keyboard operation, context-loss restore, and zero browser/console errors.
- Regressions: none in fresh recheck.

### 5 — whole artifact, async intent, and open-source boundary: holds locally

- Builder: release documentation, AGPL UI notice, pinned CI actions, dependency cleanup, source-only FFmpeg boundary, recovery quarantine, revision-aware save queue, serialized project operations, content-aware texture identity, and a single presenter/export state machine.
- Critics: open-source auditor, integration adversary, and immutable final-artifact producer.
- Initial breaks: stale/private QA ledger, unignored local traces, obsolete dependency/Node claim, incomplete AGPL notice, mutable CI tags, FFmpeg disclosure gap, stale saves winning, false autosave labels, failed hydration erasing source blobs, contradictory pin states, 202-slide self-corruption, concurrent import inversion, startup hydration beating later user intent, farthest-first mesh-pool eviction, stale same-ID textures, late presenter decode overwrites, preview-clock leakage into deterministic frames, keyboard mutation during export, phantom export locks after GPU preflight failure, silently ignored imported settings, malformed audio-only presenter acceptance, lost starter ownership, unrelated pin removal, and a presenter video that kept playing while the carousel was paused.
- Repair: public-safe ignores/docs, current dependency floor, complete legal surface, immutable CI SHAs, explicit no-binary boundary, integrity-verified recovery repackaging, strict project/settings/media semantics, ordered save/project queues, loading and dirty-state locks, commit-time slide-cap enforcement, nearest-first pool selection, content-aware caches with generation guards, export-frame texture precedence, fixed presenter time, deterministic pinned-image preload, export-inert shortcuts, post-preflight lock acquisition, persisted starter ownership, pin-owner checks, and central presenter playback truth across pause/export/context/visibility.
- Evidence: 58/58 unit tests, a clean cold 16/16 full browser run, exact hostile race/media/lifecycle replays, frozen source fingerprint, immutable production bundle, decoded 1080 × 1920 masters, and visual inspection of start/mid/end frames.
- Remaining limit: simultaneous independent browser-tab writers were not conflict-tested and are disclosed.

## Whole-artifact pass

- Cohesion: the final Road Memory capture, six authored themes, controls, demo slides, copy, fallback language, and export behavior speak one product language rather than a collection of demos.
- Cross-boundary truth: preview/export share the evaluator; H.264 is opaque; PNG carries alpha; presenter audio is accepted only where the one-frame sync promise holds; failed saved projects enter recovery instead of being overwritten.
- Real output: opaque and presenter masters are H.264 1080 × 1920, 30 fps, exactly 90 frames / 3.000 s, BT.709; AAC ends +8 ms from video; transparent PNG has mixed alpha.
- Public hygiene: source, tests, original demo art, license, asset terms, contribution/security policy, CI, and third-party notices are intended for publication. Local traces, user-media bundles, builds, and masters are excluded.

## Final state

- Local verdict: holds, 5 of 5 rounds accepted after repair.
- Checks performed: source/type/build, 58 focused tests, 16 real-browser tests, GPU pixel probes, persistence/race attacks, async latest-intent attacks, presenter clock/lifecycle checks, complete media decode/readback, responsive/accessibility interactions, privacy/secret/dependency audit, and visual artifact inspection.
- External gate: this receipt is intentionally frozen before its own GitHub commit. Repository visibility, remote SHA equality, and exact-commit CI must be read back after publication rather than guessed inside a pre-publication file.
- Actions not performed: no hosted deployment, binary release, package publication, email, purchase, or deletion.
