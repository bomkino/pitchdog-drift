# Scope boundary

This review stack changes only native product fit, local help, provenance, and diagnostic truth. It does not replace the renderer, project format, export verifier, sandbox, native file broker, or codec architecture from the parent standalone-app branch.
