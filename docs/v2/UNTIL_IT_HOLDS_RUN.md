# Until It Holds — Drift V2 run receipt

Started: 22 August 2026

Method: capped builder–critic rounds for each material gate; failures repair the causal contract, not merely the assertion.

## Outcome

Produce an honest, beautiful, deterministic Editorial Performance vertical slice as an isolated Mac development app. Preserve public and installed V1. Do not mislabel registry foundations, compatibility rendering, local packaging, or a checkpoint branch as complete donor-plan V2.

## Authority and stop conditions

- Source: `codex/v2-directors-cut`.
- Protected baseline: public V1 `5fd145207235884790ba071c5d84bc3876ff4989` and `/Applications/Drift.app`.
- Allowed: source and documentation edits, tests, browser inspection, exact-SHA V2 development packaging, and isolated installation at `/Applications/Drift V2 Dev.app`.
- Push: permitted only after the exact checkpoint holds and is labelled honestly.
- Merge to `main`: blocked for this checkpoint; the curated plan's canonical renderer and live-system gates are incomplete.
- Production identity, public binary release, Developer ID signing, notarisation, stapling, publication, and application submission remain separate actions.

## Frozen bar

- Project V4 rejects unsupported work without mutating the open project and preserves compatibility direction.
- Entry, body, exit, repeat, tempo, presenter participation, reduced motion, distance, velocity, background phase, and grain use explicit deterministic time.
- A still defaults to visible body state rather than a blank opening frame.
- The pin is protected, borderless by default, safe, source-aspect aware, focal-point aware, and visually intentional in both motion axes.
- Moving cards have no transparent-looking glass box; shadow support remains soft through its true discard threshold.
- Grain is world-only, restrained, deterministic, held at 12 fps in output, and frozen by Pause/Reduce Motion.
- Future recipe provenance cannot retain a fingerprint after its resolved World-owned domain drifts.
- V1 remains installed and usable throughout.

## Builder–critic rounds

### 1 — V2 identity

- Built a separate bundle, executable, bundle identifier, database, cache, WebKit store, and document-ownership boundary.
- Prior package passed the `3/3` packaged-WKWebView matrix and ran beside V1.
- Verdict: HOLD for identity. Existing installation is now stale and must not be used as evidence for current features.

### 2 — Project V4 and recovery

- Added strict V4 authority, pure V3/legacy migration, extension bounds, candidate-before-replacement, and recovery lock.
- Focused project browser gauntlet observed zero IndexedDB writes from rejected/future candidates and locked native imports.
- Verdict: HOLD for the checked migration scope.

### 3 — protected pin

- Separated pin identity from presenter media, added protected orthographic layout, source/custom aspect, focal point, safe inset, shadow, matte, and track ownership.
- First visual round BREAK: 32% bottom-right thumbnail overlapped the moving track and HUD.
- Repair: portrait-stage first-use defaults now use a 42% landscape or 38% portrait plane, lifted right-side anchor, broad shadow, and a strong cross-axis yield lane. Remembered/user-positioned pins remain untouched.
- Fresh visual critic: HOLD in vertical and horizontal captures. Portrait-source owner approval remains a later visual gate.

### 4 — lifecycle and tempo

- Added analytical opening/body/ending evaluation, layer stagger, body/full-scene repeats, five tempo presets plus custom envelope, presenter participation, and reduced motion.
- Critic BREAK repaired an exit-direction discontinuity and divergent preview/export background phase.
- Fresh critic: HOLD; focused `97/97`, full unit suite then green.

### 5 — Director controls and persistence

- Added independently toggleable and editable lifecycle controls, exact duration fitting, repeat count, custom tempo, transition style, and reduced-motion master.
- Critic repairs preserved transition style with transitions off, synchronized theme/reduced-motion state, exposed all-zero Custom fallback, and named Fast · Slow · Fast explicitly.
- Fresh critic: HOLD; focused `62/62` and browser reload checks green.

### 6 — visual surface and grain

- Removed accidental contain matte/glass treatment from protected overlays, extended true rounded-shadow support, quieted paper texture, and moved finishing grain into display space.
- Grain plates are frame-owned and held at 12 fps. Preview/export share background phase.
- Browser captures and lifecycle video inspected; no console errors. Visual critic held the scene after the pin repair.

### 7 — export and provenance honesty

- Generic PNG stills now default to the master midpoint; callers can request frame zero explicitly. App stills use the exact first-body midpoint.
- Removed automatic application of a registry-only Editorial recipe from App boot.
- Reconciliation now clears mismatched World-domain recipe references and makes aggregate World state Custom.
- Registry counts remain foundation-only; donor ledger remains frozen-not-ported.

### 8 — browser gauntlet repairs

- Replaced an ambiguous Duration accessibility selector with its stable control ID.
- Made cancellation testing use a deliberately throttled fixture encoder, so it proves cancellation rather than machine speed; passed three consecutive races.
- Repaired older renderer probes that sampled the intentionally blank first entry frame; they now sample deterministic body time.
- Replaced one transient reopen-notice assertion with durable readback of saved state, asset identity, dimensions, and ready renderer state.
- Focused repaired journeys pass. One final uninterrupted full browser run remains required after edits stop.

## Current evidence

- Source checkpoint `fb1773c` plus subsequent test/document repairs in the working tree.
- Unit suite: `245/245` passing.
- Typecheck: passing.
- Mac source contracts: passing.
- V2 Web build: passing.
- Pin visual critic: HOLD.
- Lifecycle critic: HOLD.
- Director-control critic: HOLD.
- Full browser suite: not yet accepted; interrupted diagnostic runs found and repaired stale entry-frame and race assumptions. Final coherent rerun pending.
- Current exact-SHA Mac package/install: pending.
- Push, merge, release, notarisation, publication: not performed.

## Whole-artifact gate still open

1. Commit the repaired test and documentation slice.
2. Run one uninterrupted `28/28` browser suite on the clean candidate.
3. Build and verify exact-SHA `Drift V2 Dev.app` including packaged `3/3` WKWebView evidence.
4. Install it in `/Applications` without touching V1; compare candidate and installed bundle.
5. Launch both apps and smoke the installed V2 lifecycle, pin, persistence, PNG, and short MP4 paths.
6. Read the installed receipt and identity back, then report package/install/push/merge/release states separately.
