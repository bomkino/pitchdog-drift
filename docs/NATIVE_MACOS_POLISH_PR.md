# Native macOS polish stack

Base branch: `feat/native-macos-studio`

This branch is intentionally stacked above the standalone-app draft. It exists to isolate native product fit, provenance, in-app help, and falsifiable diagnostics from the parent architecture work. It must remain a draft and must not publish or merge binaries.
