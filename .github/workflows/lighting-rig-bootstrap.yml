name: Build cinematic lighting rig

# Historical bootstrap retained only until the cleanup gauntlet removes it.
# It must never run automatically again: production source now exists.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Lighting bootstrap retired; use the regression gauntlet."
