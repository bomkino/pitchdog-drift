diff --git a/docs/QA_REPORT.md b/docs/QA_REPORT.md
index e72c0aa2502f253f6cb1f33ef237c9c1e23be408..c80c40eab95ea8ccb37d4a5dc3b3e99d5dedf2b0 100644
--- a/docs/QA_REPORT.md
+++ b/docs/QA_REPORT.md
@@ -1,87 +1,192 @@
-# QA receipt
+# Director’s Cut QA receipt
 
-Checked on 20 August 2026 in desktop Google Chrome 151 on macOS. This receipt separates source checks, browser behavior, decoded media, and limits; none substitutes for the others.
+Checked on **21 August 2026**. This receipt describes the recovered Director’s Cut source tree, not the older exploratory GitHub branch and not the launch-era baseline alone. It separates what is proved locally, what was proved by the original immutable browser/media run, and what still requires an exact-head GitHub Actions run.
 
-## Frozen source and browser suite
+## Verdict
 
-- Frozen application/package source fingerprint: `af6c7258b744d86834b38cfe602f1b016899db1e3b9206e0460d89281ab6557d`.
-- Immutable production bundle fingerprint: `63308fbaef88fbd9a03b069f229e2b10075b5b224f56df5996cd8c18e994b648`.
-- `npm run check`: TypeScript passed, all 58 focused Vitest checks passed, and the Vite production build passed.
-- A clean, cold `CI=1 npm run test:e2e` passed all 16 real-Chrome tests in 2.2 minutes on the frozen source.
-- The browser run covers WebGL2 boot, controls, context loss/restore, 320/390 px layouts, keyboard/file-picker access, empty/1/12/corrupt inputs, audio-only presenter rejection, saved starter replacement, presenter pause/export truth, fresh-profile portability, recovery quarantine, stale-save ordering, export lifecycle/preflight recovery, DOM fallback, straight-alpha PNG colour, focal edge reach in both axes, renderer-pool pressure, same-ID media replacement, late presenter decode, and pinned-image export outside the moving mesh pool.
-- The immutable production capture used native ANGLE Metal on an Apple M2. It recorded zero console errors and zero page errors.
+The **source gate holds locally**.
 
-## Decoded master evidence
+The **release gate remains deliberately open** until the clean branch is published and its exact head completes the complete Chromium/WebGL suite. No document, screenshot, or earlier branch run substitutes for that final clean-checkout receipt.
 
-The production UI generated the following local QA artifacts. Rendered masters stay in the ignored `artifacts/qa/` folder because the public repository is source-only.
+## Exact recovered source gate
+
+The final local tree contains only product source, tests, documentation, and the ordinary read-only CI workflow. It contains no encoded patch payload, self-editing workflow, transport workflow, generated build directory, browser trace, rendered master, or user project.
+
+Current gate:
+
+- `npm run typecheck`: passed.
+- Vitest: **16/16 files, 111/111 tests passed**.
+- `npm run build`: passed.
+- Vite transformed 113 modules and produced a valid production build.
+- Initial editor JavaScript: **349.13 kB** uncompressed / **109.86 kB** gzip.
+- Lazy cinematic evaluator: **567.84 kB** uncompressed / **142.92 kB** gzip.
+- Export studio: **528.31 kB** uncompressed / **134.06 kB** gzip.
+- AAC encoder: **991.62 kB** uncompressed / **260.35 kB** gzip.
+- The only build warnings are Vite’s existing large-chunk advisories. They are warnings, not compile failures.
+
+The lazy renderer split matters materially: project recovery, media management, the five-step directing journey, and the explicit fallback can arrive without placing Three.js in the initial editor payload.
+
+### Static contract coverage
+
+The 111 checks cover, among other things:
+
+- 12 authored film worlds that validate and differ materially rather than only by colour;
+- 14 rendered atmosphere families plus transparent output;
+- optical-profile bounds, neutral bypass, resolution invariance, presenter protection, and alpha safety;
+- deterministic backgrounds, exact cyclic phase closure, stable grain, and no reversed numeric `smoothstep` edges;
+- non-compounding deterministic takes, undo/redo, pressure profiles, and current-world recognition;
+- deck ratio, frame, and source-resolution diagnostics;
+- creator-journey navigation, guide geometry, poster timing, and destination-specific publishing truth;
+- portable-project validation, migration, asset integrity, storage races, export rollback, and media limits;
+- source hygiene: exactly one normal CI workflow, no creator-payload directory, no write-permission CI, no CI `git push`, no patch decoder, and no loading/onboarding toast that permanently covers the interface;
+- human-facing output language: **PNG poster**, **Save PNG poster**, and **Add presenter**.
+
+## Browser gauntlet
+
+The source defines **28 real-browser scenarios** spanning the complete creator path and the hostile engine/export cases.
+
+### What was exercised in the isolated recovery environment
+
+The available system Chromium cannot create WebGL or WebGL2 under the tested software and ANGLE flags. Drift correctly enters its visible DOM fallback rather than faking a cinematic canvas.
+
+Focused browser checks passed for:
+
+- the complete **Slides → World → Direct → Review → Master** route;
+- the creator shell while the lazy evaluator loads;
+- Director Commands and discoverable navigation;
+- immediate and repeated mobile journey jumps, including Master before hydration settles;
+- a 320 px notice/Commands collision gate, platform-correct shortcut labels, and explicit renderer-loading/failure truth.
+
+Renderer-only contracts now wait for `ready | restored | failed` and skip explicitly when the real WebGL evaluator is unavailable. A no-GPU machine no longer waits silently for an impossible `ready` state.
+
+A complete local 28-test WebGL verdict is **not claimed**. The isolated environment has neither the Playwright-managed Chrome binary nor a WebGL-capable system Chromium. GitHub Actions must run the whole suite after publication.
+
+### Exact-head promotion requirement
+
+The final PR may leave draft only after all of the following run against the same published SHA:
+
+1. clean checkout;
+2. `npm ci`;
+3. `npm run check`;
+4. Playwright Chromium installation;
+5. all 28 browser scenarios;
+6. uploaded visual-evidence atlas;
+7. review of desktop and mobile evidence;
+8. no console or page errors;
+9. no temporary transport files in the diff;
+10. PR description updated with the actual SHA and counts.
+
+## Creator-journey falsification
+
+The deeper pass did not stop at adding controls. It attacked the places where a first-time creator could lose confidence.
+
+### Slides
+
+- The media surface distinguishes moving slides from the optional presenter.
+- Mixed source ratios, frame mismatch, and insufficient resolution are visible.
+- “Match slide frame” is explicit and reversible.
+- Warnings are advisory. Drift never pretends to understand or redesign slide semantics.
+
+### World
+
+- The initial authored state identifies its real world instead of incorrectly reporting “Custom.”
+- Applying a world changes motion, surface, atmosphere, lens, and pacing together.
+- Transparent state cannot leak into an opaque world.
+
+### Direct
+
+- Restrained, Directed, and Heightened pressure remain bounded.
+- New Take is deterministic for a seed and does not compound on the preceding mutation.
+- Undo and redo operate on whole directing decisions.
+- Clean Lens is a reversible comparison, not a destructive reset.
+
+### Review
+
+- Review opens the actual review destination and restores focus truth.
+- Composition and social-safe guides never enter exports.
+- Poster time is saved and validated.
+- Readability pressure names risky motion and optics without claiming semantic legibility analysis.
+
+### Master
+
+- MP4, PNG poster, and PNG sequence are evaluated independently.
+- A transparent scene blocks opaque H.264 without invalidating valid transparent PNG destinations.
+- Export state belongs to one session; stale completion timers cannot erase or corrupt a later session.
+- Completed output reads **Verified** only after readback.
+
+## Historical immutable media evidence
+
+The launch baseline produced immutable local evidence before the Director’s Cut expansion. It remains relevant to the unchanged export foundation, but it is not represented as proof of the unpublished final source tree.
+
+That baseline included:
 
 | Artifact | Direct readback |
 | --- | --- |
-| Opaque master | H.264, 1080 × 1920, yuv420p, exact 30/1 fps, 90/90 decoded frames, 3.000 s, BT.709 primaries/transfer/matrix, no audio |
-| Presenter master | H.264 video: 1080 × 1920, 30/1 fps, 90/90 frames, 3.000 s, BT.709. AAC: 48 kHz stereo, 3.008 s. End offset +8 ms, below one 30 fps frame (33.333 ms) |
-| Transparent still | PNG, 1080 × 1920, RGBA. Alpha min 0, max 1, mean 0.208331, proving both transparent and visible pixels |
-| Public screenshot | PNG, 1440 × 900, captured from the same immutable production run |
+| Opaque master | H.264, 1080 × 1920, yuv420p, exact 30/1 fps, 90/90 decoded frames, 3.000 s, BT.709, no audio |
+| Presenter master | H.264 1080 × 1920, 30/1 fps, 90/90 frames, 3.000 s; AAC 48 kHz stereo, 3.008 s; +8 ms end offset |
+| Transparent still | PNG 1080 × 1920 RGBA with both transparent and visible pixels |
 
-SHA-256:
+Baseline SHA-256 values:
 
 ```text
 5ce68413dad7f96c927e9817433717c21c594b8bded7155424c38a48490604a2  opaque MP4
 a531608d26e6473d52771f73549851d0ca4560e17305ba8a0919ac851f4c26ef  presenter MP4
 c51d2240ba7e502f0e6d74c78a8fdaa1a98863754128943e5e7f6be4e01f2560  transparent PNG
-fcede1f6cda77717bfba2f5a57c1cafb2c5e6a370f4618bf8d0ed2fa761c38a0  public screenshot
+fcede1f6cda77717bfba2f5a57c1cafb2c5e6a370f4618bf8d0ed2fa761c38a0  baseline public screenshot
 ```
 
-Both MP4s passed complete ffmpeg decode with `-xerror`. Frames 0, 45, and 89 were extracted from each master and visually inspected. The moving deck changes across time without black or corrupt frames; the presenter remains pinned while its synthetic mouth state changes, proving time-indexed video decode rather than a frozen poster frame.
+Both baseline MP4s completed strict ffmpeg decode. Start, midpoint, and final frames were visually inspected. The presenter fixture changed over time while remaining pinned.
 
-Machine-readable local receipts are `artifacts/qa/final-media-verification.json`, `artifacts/qa/final-ui-evidence.json`, and `artifacts/qa/final-sha256-manifest.txt`.
+## Renderer and export invariants retained
 
-## Renderer falsification
+- Preview and every export route use the same evaluator.
+- H.264 is always opaque; transparency belongs to PNG.
+- Shader colour is encoded once for the sRGB output target.
+- Straight-alpha capture avoids dark fringes and double premultiplication.
+- Output dimensions are rejected against actual GPU limits before renderer state changes.
+- The bounded mesh pool preserves the focal neighbourhood under pathological ratios.
+- Same-ID replacement keys include verified content identity, preventing stale texture reuse.
+- Late async media cannot overwrite newer presenter or export intent.
+- Reduced motion removes temporal instability while preserving static art direction.
+- Seamless output closes carousel distance, atmosphere phase, and optical phase on the same mathematical boundary.
 
-- sRGB regression probe: `#808080` rendered and captured as exact `[128, 128, 128, 255]`; the production MP4 remained within expected codec quantization and carried BT.709 tags.
-- Cover focal controls reached the source’s left/top, center, and right/bottom content. Contain preserved the complete source and placed its letterbox band at the requested screen edge.
-- A half-alpha gray source round-tripped as `[128, 128, 128, 128]` and composited over white as `[191, 191, 191]`, closing the dark-fringe/double-premultiplication failure.
-- All 24 background-style × fit × axis shader paths compiled without a GL error in the targeted browser probe.
-- Oversized output was rejected against real GPU limits before renderer or canvas state changed.
-- Extreme custom ratios could not evict the centered slide from the 24-mesh renderer pool.
-- Replacing an asset with the same project ID but different verified bytes changed the rendered texture; a delayed older presenter decode could not overwrite later preview or fixed-step export intent.
-- A pinned image outside the selected moving pool was decoded and bound before its export frame. Presenter grain and animation use the evaluated frame time, not the preview clock.
+## Responsive, accessible, and fallback truth
 
-## Projects and recovery falsification
+The interface has explicit desktop and compact modes. The compact journey jump now activates the correct panel, waits until the destination exists, scrolls it into view, and restores focus. Master no longer opens Stage while aiming at hidden diagnostics.
 
-The persistence module passed eight focused tests plus an isolated Chrome harness for save/load/export/clear, import-without-mutation, and corrupt-import preservation. The integrated app then survived hostile browser replays that ordinary happy-path tests had missed:
+The current compact visual pass also removed a redundant onboarding notification that covered controls on a 390 × 844 viewport.
 
-- a delayed older autosave could not overwrite a newer imported project;
-- delayed import A followed by import B reopened as B;
-- delayed startup hydration A followed by early user import B showed a truthful loading lock, then reopened as B;
-- overlapping 101 + 101 image batches decoded only remaining capacity, stopped at 200, saved, and reopened normally;
-- an unsupported saved project remained quarantined instead of being replaced by fallback demos;
-- failed storage of a valid replacement restored the fallback view and retained `recovery locked`; when the locked storage was already integrity-verified, recovery re-verified and repackaged the preserved manifest and media without replacing IndexedDB (the resulting ZIP is not claimed to be byte-identical to an earlier archive);
-- malformed engine/theme/settings/descriptors/references and contradictory pin states were rejected without replacement;
-- saved starter-study ownership survived reload, so the first real deck still replaced the studies instead of mixing with them;
-- removing an unpinned presenter video preserved an unrelated pinned slide;
-- an audio-only MP4 and invalid video metadata were rejected before project mutation;
-- presenter playback paused with the carousel, export, context loss, and document hiding, then restored to the truthful prior state.
+When WebGL2 is denied:
 
-## Responsive and fallback surfaces
+- slides and presenter media remain manageable;
+- project import, recovery, and portable backups remain available;
+- the lazy evaluator reports failure explicitly;
+- playback and cinematic export controls disable honestly;
+- the product does not substitute a CSS imitation and call it a rendered master.
 
-Direct browser checks passed at 1440 × 900, 1024 × 768, 390 × 844, and 320 × 568. Below 1120 px, the interface becomes explicit Media / Stage / Director panels. At 320 and 390 px the document width and height remain equal to the viewport, the footer stays reachable, hidden file inputs stay outside Tab order, reorder controls work from the keyboard, and segmented controls expose visible focus.
+## Repository and privacy hygiene
 
-With WebGL2 denied, media, themes, and portable projects remain usable. MP4 export fails before opening a destination picker and announces that the cinematic renderer is unavailable.
+- One ordinary CI workflow only.
+- CI uses read-only repository permissions.
+- No workflow commits or pushes into its own PR branch.
+- No encoded source or patch payloads.
+- No analytics, cloud upload, remote font, runtime API, or hidden network request.
+- Generated builds, browser traces, rendered masters, local projects, environment files, and QA artifacts remain ignored.
+- Project source is AGPL-3.0-or-later; original study assets are CC BY-SA 4.0.
 
-## Release hygiene
+## Known limits
 
-- `npm audit` reports zero known vulnerabilities at the checked lockfile.
-- Runtime source contains no analytics, remote font, cloud upload, fetch/XHR/WebSocket client, or embedded secret.
-- CI actions are pinned to immutable commit SHAs and use read-only repository permissions.
-- Local browser traces, rendered masters, portable projects, build output, and environment files are ignored.
-- The repository publishes source only. Compiled distribution remains blocked until the embedded FFmpeg WASM provenance and LGPL corresponding-source/relink obligations are satisfied.
+- Complete export remains a current desktop Chrome target. Brave is capability-gated, not independently certified here.
+- Moving-track video remains outside v1. One pinned presenter video is supported.
+- Presenter audio is accepted at 24/25/30 fps; 50/60 fps requires mute until the browser AAC timing contract can be proved.
+- Portable projects remain capped at 96 MiB archive input, 80 MiB total source assets, and 64 MiB per asset.
+- Large PNG sequences should stream to a directory; unsafe in-memory ZIP estimates are rejected.
+- Independent simultaneous tabs writing the same IndexedDB project are not conflict-certified.
+- The tool can report visual pressure and source health. It cannot certify the narrative quality or semantic legibility of an arbitrary deck.
 
-## Known, deliberate limits
+## Final release status
 
-- Complete export behavior is verified in current desktop Chrome. Brave is a capability-gated target, not an independently verified runtime in this receipt.
-- Moving-track video is not v1 scope. One pinned presenter video is.
-- Presenter audio masters are limited to 24/25/30 fps. Muted presenter video may export at 50/60 fps.
-- Portable assets are capped at 80 MiB total / 64 MiB each; archive input is capped at 96 MiB.
-- Full 1080 × 1920 PNG sequences should stream to a directory. The in-memory ZIP route rejects unsafe estimates.
-- Simultaneous independent tabs editing the same IndexedDB project were not conflict-tested; use one editor tab and portable backups.
-- These checks prove the tool and included study. They do not pre-approve the pacing or legibility of a future user deck.
+- Local source gate: **pass**.
+- Exact final clean-checkout WebGL gate: **pending publication**.
+- Human visual acceptance of exact final evidence: **pending publication**.
+- Merge, deployment, package publication, binary release, and auto-merge: **not performed**.
diff --git a/docs/UNTIL_IT_HOLDS.md b/docs/UNTIL_IT_HOLDS.md
index 90594eb8bdeafafa3d7df99c3649e4b2367076eb..dfcc5ce4a61af07c740f440e5ebe5747557ce55b 100644
--- a/docs/UNTIL_IT_HOLDS.md
+++ b/docs/UNTIL_IT_HOLDS.md
@@ -1,71 +1,179 @@
-# Until It Holds — run receipt
+# Until It Holds — Director’s Cut receipt
 
-- Started: 20 August 2026, Asia/Kolkata
-- Total-attempt cap: 5
-- Outcome: A controllable cinematic pitch-slide production tool whose real captures survive technical and creative scrutiny.
-- Artifact: this repository
-- Authority: local reversible work, read-only research, and final public source-repository publication are allowed. No website deployment, binary release, email, purchase, or source deletion.
-- Baseline: empty, non-git folder.
+- Date: 20 August 2026
+- Artifact: Drift source repository
+- Actual goal: make pitch-deck slides feel filmed, directed, and trustworthy while a first-time creator can reach a credible social master without understanding renderer architecture.
+- Hard boundary: no merge, deployment, release, purchase, email, or destructive repository action.
+- Final route: **Slides → World → Direct → Review → Master**.
 
-The frozen product bar is in `docs/PRODUCT_CONTRACT.md`. A critic verdict is recorded as evidence, never used as a substitute for inspecting the artifact.
+A round “holds” only when its evidence survives an adversarial question. Prose, screenshots, and earlier branch checks are not substitutes for the current artifact.
 
-## Rounds
+## Round 1 — product contract
 
-### 1 — research and product contract: holds
+**Break:** A WebGL carousel demo could look impressive while failing the real job: importing a deck, directing it quickly, keeping the slide readable, and producing a trustworthy master.
 
-- Builder: froze the real product as an Instagram-master tool, not a carousel demo; separated preview, deterministic output, alpha, persistence, presenter decode, and fallback promises.
-- Critic: reference researcher plus technical-product architect.
-- Initial break: contract blurred moving-track video scope, universal MP4 support, alpha output, reduced-motion export, and portable recovery.
-- Repair: image-only moving track, one optional presenter decoder, current-Chrome export target, honest PNG alpha, fixed 1080 × 1920 master defaults, fresh-profile bundle proof, and visible failure boundaries.
-- Evidence: primary reference study and the frozen contract.
-- Decision: borrow principles and mechanisms, never compositions.
+**Repair:** The product contract now separates:
 
-### 2 — renderer and motion engine: holds
+- moving slides from the optional presenter;
+- preview from deterministic output;
+- H.264 from alpha-capable PNG;
+- renderer capability from project availability;
+- visual-pressure advice from false semantic analysis;
+- authored worlds from random parameter collections.
 
-- Builder: pure time evaluator, Three.js/WebGL2 scene, raw GLSL slides/backgrounds, five spatial flows, bounded optical distortion, continuous corners, pinned frame, and GPU limit preflight.
-- Critic: hostile shader/engine pixel review in real Chrome.
-- Initial breaks: linear-dark custom shaders, inverted contain math, false focal movement, stale transparency after themes, double-premultiplied transparent RGB, and inverted vertical focal semantics.
-- Repair: Three color-space output chunks, aspect-derived cover/contain/focal math, top-left UI mapping, straight-alpha capture, explicit theme transparency, and preflight before canvas mutation.
-- Evidence: exact mid-gray and half-alpha pixels, source-edge focal probes in both axes, 24 compiled shader paths, BT.709 MP4 samples, and unchanged renderer state after oversize rejection.
-- Regressions: none in targeted recheck.
+**Decision:** holds.
 
-### 3 — deterministic export and durable projects: holds
+## Round 2 — cinematic optics
 
-- Builder: fixed-step MP4/PNG exporters, presenter sample decode, software AAC path, container readback, rollback-aware disk targets, IndexedDB storage, and hashed `.pitched` bundles.
-- Critics: independent export and persistence adversaries.
-- Initial breaks: cancellation after finalisation, wrong A/V interval checks, absent MP4 readback, timezone-unstable ZIP metadata, weak directory cleanup, AAC priming tail, and presenter freeze outside its source duration.
-- Repair: cancellation-aware targets, decoded container verification, strict audio coverage/tail gates, capped 24/25/30 fps audio masters, exact PNG sequence checks, stable archive metadata, and strict presenter sample availability.
-- Evidence: 21 export tests, eight persistence tests, real-Chrome streaming/cancel/alpha/AAC probes, exact 240-frame default proof, and fresh-profile project reopen.
-- Regressions: none in module rechecks.
+**Break:** Per-card deformation alone could not make the composition feel photographed through one coherent lens. Blanket blur would destroy text, presenter clarity, alpha truth, and performance.
 
-### 4 — controls, browser fallback, and accessibility: holds
+**Repair:** One deterministic scene-level optical pass now coordinates:
 
-- Builder: authored six-theme director, responsive three-panel shell, keyboard reorder, visible focus, honest file inputs, 24/25/30/50/60 fps controls, and DOM fallback.
-- Critics: fresh responsive/accessibility and visual reviewers.
-- Initial breaks: theme transparency leaked, mobile radio inputs expanded the page, focus was invisible, file inputs were tabbable offscreen, reorder was drag-only, and fallback MP4 opened a destination picker before failing.
-- Repair: coherent theme reset, contained radios, focus-visible treatment, hidden non-Tab inputs with visible triggers, keyboard up/down controls, exact mobile containment, and renderer preflight before any picker.
-- Evidence: 320/390 px viewport equality, stable footer, picker instrumentation, keyboard operation, context-loss restore, and zero browser/console errors.
-- Regressions: none in fresh recheck.
+- centre-protected soft focus;
+- edge defocus;
+- velocity-aware smear;
+- bounded chromatic separation;
+- bloom and warm halation;
+- anamorphic flare;
+- barrel/pincushion response;
+- vignette, grain, gate weave, and breathing;
+- an optional protected presenter layer;
+- a true neutral bypass.
 
-### 5 — whole artifact, async intent, and open-source boundary: holds locally
+Preview, poster, sequence, and MP4 use the same evaluator.
 
-- Builder: release documentation, AGPL UI notice, pinned CI actions, dependency cleanup, source-only FFmpeg boundary, recovery quarantine, revision-aware save queue, serialized project operations, content-aware texture identity, and a single presenter/export state machine.
-- Critics: open-source auditor, integration adversary, and immutable final-artifact producer.
-- Initial breaks: stale/private QA ledger, unignored local traces, obsolete dependency/Node claim, incomplete AGPL notice, mutable CI tags, FFmpeg disclosure gap, stale saves winning, false autosave labels, failed hydration erasing source blobs, contradictory pin states, 202-slide self-corruption, concurrent import inversion, startup hydration beating later user intent, farthest-first mesh-pool eviction, stale same-ID textures, late presenter decode overwrites, preview-clock leakage into deterministic frames, keyboard mutation during export, phantom export locks after GPU preflight failure, silently ignored imported settings, malformed audio-only presenter acceptance, lost starter ownership, unrelated pin removal, and a presenter video that kept playing while the carousel was paused.
-- Repair: public-safe ignores/docs, current dependency floor, complete legal surface, immutable CI SHAs, explicit no-binary boundary, integrity-verified recovery repackaging, strict project/settings/media semantics, ordered save/project queues, loading and dirty-state locks, commit-time slide-cap enforcement, nearest-first pool selection, content-aware caches with generation guards, export-frame texture precedence, fixed presenter time, deterministic pinned-image preload, export-inert shortcuts, post-preflight lock acquisition, persisted starter ownership, pin-owner checks, and central presenter playback truth across pause/export/context/visibility.
-- Evidence: 58/58 unit tests, a clean cold 16/16 full browser run, exact hostile race/media/lifecycle replays, frozen source fingerprint, immutable production bundle, decoded 1080 × 1920 masters, and visual inspection of start/mid/end frames.
-- Remaining limit: simultaneous independent browser-tab writers were not conflict-tested and are disclosed.
+**Decision:** holds at the source-contract level; exact final WebGL pixels remain a publication gate.
 
-## Whole-artifact pass
+## Round 3 — worlds and atmosphere
 
-- Cohesion: the final Road Memory capture, six authored themes, controls, demo slides, copy, fallback language, and export behavior speak one product language rather than a collection of demos.
-- Cross-boundary truth: preview/export share the evaluator; H.264 is opaque; PNG carries alpha; presenter audio is accepted only where the one-frame sync promise holds; failed saved projects enter recovery instead of being overwritten.
-- Real output: opaque and presenter masters are H.264 1080 × 1920, 30 fps, exactly 90 frames / 3.000 s, BT.709; AAC ends +8 ms from video; transparent PNG has mixed alpha.
-- Public hygiene: source, tests, original demo art, license, asset terms, contribution/security policy, CI, and third-party notices are intended for publication. Local traces, user-media bundles, builds, and masters are excluded.
+**Break:** More presets can still mean more colourways. Procedural backgrounds can become wallpaper, visible tiling, noisy shaders, or non-closing loops.
 
-## Final state
+**Repair:** Drift now carries:
 
-- Local verdict: holds, 5 of 5 rounds accepted after repair.
-- Checks performed: source/type/build, 58 focused tests, 16 real-browser tests, GPU pixel probes, persistence/race attacks, async latest-intent attacks, presenter clock/lifecycle checks, complete media decode/readback, responsive/accessibility interactions, privacy/secret/dependency audit, and visual artifact inspection.
-- External gate: this receipt is intentionally frozen before its own GitHub commit. Repository visibility, remote SHA equality, and exact-commit CI must be read back after publication rather than guessed inside a pre-publication file.
-- Actions not performed: no hosted deployment, binary release, package publication, email, purchase, or deletion.
+- 12 authored film worlds whose motion, surface, atmosphere, and lens direction move together;
+- 14 procedural atmosphere families plus transparency;
+- exact cyclic phase motion;
+- deterministic recuts that preserve identity;
+- static seeded grain under pause/reduced motion;
+- no reversed numeric `smoothstep` edges;
+- no stale transparent state in opaque worlds.
+
+Night Run was rebuilt after visual review rejected its regular orange dash field. The replacement uses a vanishing road, sparse paired lights, wet reflections, horizon bloom, and restrained flare.
+
+**Decision:** holds in unit/shader contracts; the exact final visual atlas remains a human review gate.
+
+## Round 4 — the creator journey
+
+**Break:** A technically rich inspector still asked the user to understand the engine. The first draft made “more controls” the product.
+
+**Repair:** The interface now exposes a directed route:
+
+1. **Slides** — import and inspect ratio, frame, and resolution truth.
+2. **World** — begin from an authored direction.
+3. **Direct** — choose pressure, generate non-compounding takes, undo, redo, and compare clean glass.
+4. **Review** — hold the exact moment, inspect guides, poster time, deck health, and readability pressure.
+5. **Master** — choose a destination and repair only the blockers relevant to it.
+
+Director Commands lets a user search in creator language rather than memorise panel architecture.
+
+The compact navigation was attacked directly. Review and Master now activate the real panel, wait for the destination, scroll it into view, and restore focus. A redundant first-load toast that covered mobile controls was removed.
+
+**Decision:** holds in focused browser and source-contract tests.
+
+## Round 5 — publishing truth
+
+**Break:** Treating H.264 as the only legitimate result made transparent projects look globally broken even when PNG poster and PNG sequence were valid. Stale export timers could also interfere with a later session.
+
+**Repair:** Publishing readiness is destination-specific:
+
+- scene readiness;
+- MP4 readiness;
+- PNG poster readiness;
+- PNG sequence readiness.
+
+A codec limitation cannot invalidate another valid deliverable. Poster time is explicit and persisted. Export sessions own their abort controller, timers, and completion state. Output reads **Verified** only after readback.
+
+**Decision:** holds in 111 static/unit contracts and the retained immutable baseline media evidence; exact final exported media remains a clean-head browser gate.
+
+## Round 6 — project safety and failure states
+
+**Break:** Local-first tools often become data traps: stale autosaves, corrupted imports, hidden limits, overwriting recovery data, late media races, or dead controls on unsupported hardware.
+
+**Repair:** The project and renderer boundaries now include:
+
+- SHA-256 source identity;
+- strict schema validation and migration;
+- latest-intent guards for saves, imports, hydration, and media decode;
+- quarantine instead of silent replacement;
+- explicit archive and asset limits;
+- rollback-aware export destinations;
+- conservative GPU surface preflight;
+- bounded mesh/texture pools;
+- honest loading, ready, lost, restored, and failed renderer states;
+- a usable DOM fallback that never claims cinematic output.
+
+**Decision:** holds in source and focused fallback-browser checks.
+
+## Round 7 — performance, accessibility, and language
+
+**Break:** The heavy renderer delayed the whole product, compact navigation could focus invisible content, and implementation language leaked into creator-facing copy.
+
+**Repair:**
+
+- Three.js and the cinematic evaluator now load lazily.
+- Initial editor JS is 349.13 kB / 109.86 kB gzip.
+- The shell and recovery path arrive before the renderer.
+- Compact journey navigation re-anchors after late hydration/layout shifts and preserves visibility and focus truth.
+- Hidden file inputs stay outside keyboard order.
+- Output language now says **PNG poster**, not “transparent-safe still.”
+- The media action now says **Add presenter**.
+- Permanent loading/onboarding toasts are rejected by source-contract tests.
+
+**Decision:** holds locally.
+
+## Round 8 — repository hygiene and proof
+
+**Break:** The interrupted run left encoded payload fragments and self-mutating CI on the GitHub branch. Even good product code becomes unreviewable when transport machinery pollutes the diff or CI modifies its own head.
+
+**Repair in the final local tree:**
+
+- exactly one ordinary CI workflow;
+- read-only CI permissions;
+- no `git push` from CI;
+- no patch decoder;
+- no creator-payload directory;
+- no generated build, trace, project, or master in source;
+- source-contract tests that permanently reject those regressions.
+
+Current exact local receipt:
+
+- TypeScript: pass;
+- Vitest: 16/16 files, **111/111 tests**;
+- production build: pass;
+- focused creator/fallback browser checks: pass;
+- full 28-scenario WebGL run on exact final SHA: pending publication;
+- exact final visual-evidence review: pending publication.
+
+**Decision:** product source holds locally. Publication does not yet hold.
+
+## Current final state
+
+What is finished:
+
+- the deeper cinematic system;
+- the complete creator journey;
+- mobile/focus repair;
+- destination-specific publishing truth;
+- lazy renderer and honest fallback;
+- source hygiene gates;
+- 111 passing static/unit contracts;
+- production build.
+
+What cannot be truthfully claimed before publication:
+
+- an exact-head GitHub Actions pass;
+- a complete exact-head WebGL/Chromium pass;
+- human approval of the exact-head visual atlas;
+- a clean GitHub PR without the interrupted transport debris.
+
+No merge or release action has been performed. The artifact should remain draft until those three external gates close.
diff --git a/e2e/cleanLensComparison.e2e.ts b/e2e/cleanLensComparison.e2e.ts
index 7b5d5acd7e0d07eef8a528345c741bd2f0f3cd8f..95f78ed4a1e54adcb520f68c4424762824cd3caf 100644
--- a/e2e/cleanLensComparison.e2e.ts
+++ b/e2e/cleanLensComparison.e2e.ts
@@ -7,22 +7,30 @@ test("Clean lens is an honest reversible comparison, not a destructive reset", a
   await page.emulateMedia({ reducedMotion: "reduce" });
   await page.goto("/");
 
+  const frame = page.locator(".stage-frame");
+  await expect(frame).toHaveAttribute("data-context", /ready|restored|failed/, { timeout: 30_000 });
+  if (await frame.getAttribute("data-context") === "failed") {
+    test.skip(true, "This optical contract requires the real WebGL evaluator.");
+  }
+
   const worldSelect = page
     .locator("select")
     .filter({ has: page.locator("option", { hasText: "Editorial Drift" }) })
     .first();
   const options = await worldSelect.locator("option").evaluateAll((nodes) =>
-  nodes.map((node) => ({
-    value: (node as HTMLOptionElement).value,
-    label: (node.textContent ?? "").trim(),
-  })).filter((option) => option.value && option.label),
-);
-const expressiveWorld = options.find((option) => /Dread|Chrome Dream|Night Run/i.test(option.label));
-expect(expressiveWorld, "A strongly treated authored world is required for comparison QA").toBeTruthy();
-await worldSelect.selectOption(expressiveWorld!.value);
+    nodes
+      .map((node) => ({
+        value: (node as HTMLOptionElement).value,
+        label: (node.textContent ?? "").trim(),
+      }))
+      .filter((option) => option.value && option.label),
+  );
+  const expressiveWorld = options.find((option) => /Dread|Chrome Dream|Night Run/i.test(option.label));
+  expect(expressiveWorld, "A strongly treated authored world is required for comparison QA").toBeTruthy();
+  await worldSelect.selectOption(expressiveWorld!.value);
 
   const pause = page.getByRole("button", { name: /pause preview/i });
-  if (await pause.isVisible().catch(() => false)) await pause.click();
+  if (await pause.isEnabled().catch(() => false)) await pause.click();
 
   const canvas = page.locator("canvas").first();
   await page.waitForTimeout(220);
@@ -38,7 +46,8 @@ await worldSelect.selectOption(expressiveWorld!.value);
   expect(digest(clean), "Clean lens should expose a visibly different optical treatment").not.toBe(digest(directed));
   expect(await worldSelect.inputValue(), "Comparison must not replace the selected world").toBe(selectedWorld);
 
-  const restore = page.getByRole("button", { name: /restore|return|directed lens|clean lens/i }).first();
+  const restore = page.getByRole("button", { name: "Restore directed lens", exact: true });
+  await expect(restore).toBeVisible();
   await restore.click();
   await page.waitForTimeout(180);
   const restored = await canvas.screenshot({ animations: "disabled" });
diff --git a/e2e/creatorJourney.e2e.ts b/e2e/creatorJourney.e2e.ts
index 4e75edc88aec94b072a4aa5b22be6574763b1cb7..631fb8d10d8f86c9f5080861106efb744e542a76 100644
--- a/e2e/creatorJourney.e2e.ts
+++ b/e2e/creatorJourney.e2e.ts
@@ -18,6 +18,16 @@ test("the live product carries a creator through Slides → World → Direct →
   await expect(journey.getByRole("button", { name: /Direct/i })).toContainText("Directed");
 
   await page.setViewportSize({ width: 390, height: 844 });
+
+  // Journey navigation must settle immediately even when the target panel was
+  // previously hidden and the destination sits several viewports away.
+  await journey.getByRole("button", { name: /Master/i }).click();
+  await expect(page.locator('[data-journey-section="master"]')).toBeFocused();
+  expect(await page.locator('[data-journey-section="master"]').evaluate((element) => Math.round(element.getBoundingClientRect().top))).toBeLessThan(190);
+  await journey.getByRole("button", { name: /World/i }).click();
+  await expect(page.locator('[data-journey-section="world"]')).toBeFocused();
+  expect(await page.locator('[data-journey-section="world"]').evaluate((element) => Math.round(element.getBoundingClientRect().top))).toBeLessThan(190);
+
   await journey.getByRole("button", { name: /Review/i }).click();
   await expect(page.getByRole("button", { name: "director", exact: true })).toHaveAttribute("aria-pressed", "true");
   await expect(page.locator('[data-journey-section="review"]')).toBeFocused();
@@ -61,7 +71,7 @@ test("the live product carries a creator through Slides → World → Direct →
     await atmosphere.locator("summary").click();
     await page.getByRole("combobox", { name: "Background", exact: true }).selectOption("transparent");
     await expect(page.getByRole("button", { name: "Export MP4 master" })).toBeDisabled();
-    await expect(page.getByRole("button", { name: "Save transparent-safe PNG" })).toBeEnabled();
+    await expect(page.getByRole("button", { name: "Save PNG poster" })).toBeEnabled();
     await expect(page.getByRole("button", { name: "Export PNG sequence" })).toBeEnabled();
     await expect(journey.getByRole("button", { name: /Master/i })).toContainText("2/3 paths");
   }
@@ -100,3 +110,66 @@ test("the creator shell stays useful while the heavy cinematic evaluator loads",
 
   await expect(page.locator(".stage-frame")).not.toHaveAttribute("data-context", "loading", { timeout: 30_000 });
 });
+
+test("every journey step survives immediate mobile jumps and late layout shifts", async ({ page }) => {
+  await page.setViewportSize({ width: 390, height: 844 });
+  await page.goto("/");
+
+  const journey = page.getByRole("navigation", { name: "Creator journey" });
+  await expect(journey).toBeVisible();
+
+  const assertAnchored = async (step: "world" | "direct" | "review" | "master") => {
+    const destination = page.locator(`[data-journey-section="${step}"]`);
+    await expect(destination).toBeFocused();
+    await expect.poll(async () => destination.evaluate((element) => Math.round(element.getBoundingClientRect().top)), {
+      timeout: 3_000,
+      message: `${step} should remain anchored after asynchronous layout settles`,
+    }).toBeGreaterThanOrEqual(120);
+    expect(await destination.evaluate((element) => Math.round(element.getBoundingClientRect().top))).toBeLessThan(190);
+  };
+
+  // Tap before demo assets, codec probing, and the lazy renderer have finished.
+  await journey.getByRole("button", { name: /Master/i }).click();
+  await assertAnchored("master");
+  await expect(page.locator(".asset-list li").first()).toBeAttached({ timeout: 30_000 });
+  await page.waitForTimeout(1_300);
+  expect(await page.locator('[data-journey-section="master"]').evaluate((element) => Math.round(element.getBoundingClientRect().top))).toBeLessThan(190);
+
+  for (const step of ["World", "Direct", "Review", "Master"] as const) {
+    await journey.getByRole("button", { name: new RegExp(step, "i") }).click();
+    await assertAnchored(step.toLowerCase() as "world" | "direct" | "review" | "master");
+  }
+
+  await journey.getByRole("button", { name: /Slides/i }).click();
+  await expect(page.getByRole("button", { name: "media", exact: true })).toHaveAttribute("aria-pressed", "true");
+  await expect(page.getByRole("heading", { name: "Your deck." })).toBeVisible();
+});
+
+test("mobile notices and Director Commands never cover each other", async ({ page }) => {
+  await page.setViewportSize({ width: 320, height: 720 });
+  await page.goto("/");
+
+  const journey = page.getByRole("navigation", { name: "Creator journey" });
+  await expect(journey).toBeVisible();
+  await journey.getByRole("button", { name: /Direct/i }).click();
+  await page.getByRole("button", { name: /Push the cut Heightened/i }).click();
+
+  const notice = page.locator(".notice");
+  const launcher = page.locator(".director-command-launcher");
+  await expect(notice).toBeVisible();
+  await expect(launcher).toBeVisible();
+
+  const [noticeBox, launcherBox] = await Promise.all([notice.boundingBox(), launcher.boundingBox()]);
+  expect(noticeBox).not.toBeNull();
+  expect(launcherBox).not.toBeNull();
+  const overlaps = !(
+    noticeBox!.x + noticeBox!.width <= launcherBox!.x
+    || launcherBox!.x + launcherBox!.width <= noticeBox!.x
+    || noticeBox!.y + noticeBox!.height <= launcherBox!.y
+    || launcherBox!.y + launcherBox!.height <= noticeBox!.y
+  );
+  expect(overlaps).toBe(false);
+  expect(launcherBox!.width).toBeLessThanOrEqual(42);
+  expect(noticeBox!.x).toBeGreaterThanOrEqual(0);
+  expect(noticeBox!.x + noticeBox!.width).toBeLessThanOrEqual(320);
+});
diff --git a/e2e/directorCommands.e2e.ts b/e2e/directorCommands.e2e.ts
index 76c79730826128a2ed61e36e97b19d1a09178ba9..94ce1a7246c0d70782bbeb00f42cb580dc6704d1 100644
--- a/e2e/directorCommands.e2e.ts
+++ b/e2e/directorCommands.e2e.ts
@@ -2,7 +2,8 @@ import { expect, test } from "@playwright/test";
 
 test("director commands expose the complete creator path", async ({ page }) => {
   await page.goto("/");
-  await expect(page.locator("canvas").first()).toBeVisible();
+  await expect(page.locator(".stage-frame")).toBeVisible();
+  await expect(page.getByRole("navigation", { name: "Creator journey" })).toBeVisible();
   const launcher = page.getByRole("button", { name: /Commands/i });
   await expect(launcher).toBeVisible();
   await launcher.click();
@@ -23,4 +24,14 @@ test("director commands expose the complete creator path", async ({ page }) => {
   await page.keyboard.press("Control+K");
   await expect(dialog).toBeVisible();
   await page.keyboard.press("Escape");
+
+  await page.setViewportSize({ width: 390, height: 844 });
+  await expect(launcher).toContainText("Commands");
+  const [launcherBox, transportBox] = await Promise.all([
+    launcher.boundingBox(),
+    page.locator(".transport").boundingBox(),
+  ]);
+  expect(launcherBox).not.toBeNull();
+  expect(transportBox).not.toBeNull();
+  expect(launcherBox!.y + launcherBox!.height).toBeLessThanOrEqual(transportBox!.y - 4);
 });
diff --git a/e2e/staticOpticsDeterminism.e2e.ts b/e2e/staticOpticsDeterminism.e2e.ts
index 343da3bcdb795b9601534fc5f0eac3b80dd4955d..6a8c4dc5686c39e1237cc66a15d77d31d7b78704 100644
--- a/e2e/staticOpticsDeterminism.e2e.ts
+++ b/e2e/staticOpticsDeterminism.e2e.ts
@@ -7,6 +7,12 @@ test("paused reduced-motion worlds are pixel-stable rather than quietly shimmeri
   await page.emulateMedia({ reducedMotion: "reduce" });
   await page.goto("/");
 
+  const frame = page.locator(".stage-frame");
+  await expect(frame).toHaveAttribute("data-context", /ready|restored|failed/, { timeout: 30_000 });
+  if (await frame.getAttribute("data-context") === "failed") {
+    test.skip(true, "This optical contract requires the real WebGL evaluator.");
+  }
+
   const worldSelect = page
     .locator("select")
     .filter({ has: page.locator("option", { hasText: "Editorial Drift" }) })
@@ -14,7 +20,7 @@ test("paused reduced-motion worlds are pixel-stable rather than quietly shimmeri
   await expect(worldSelect).toBeVisible();
 
   const pause = page.getByRole("button", { name: /pause preview/i });
-  if (await pause.isVisible().catch(() => false)) await pause.click();
+  if (await pause.isEnabled().catch(() => false)) await pause.click();
 
   const options = await worldSelect.locator("option").evaluateAll((nodes) =>
     nodes.map((node) => ({
diff --git a/e2e/studio.e2e.ts b/e2e/studio.e2e.ts
index a3e83a465962478631e8ad6fc9ea929b4e4f6744..95edfe0921f5fbf65e8bc7e1f786cebe15c7357f 100644
--- a/e2e/studio.e2e.ts
+++ b/e2e/studio.e2e.ts
@@ -7,10 +7,19 @@ const audioOnlyFixturePath = path.resolve("e2e/fixtures/audio-only.mp4");
 const presenterFixturePath = path.resolve("e2e/fixtures/presenter.mp4");
 const halfAlphaGreyPng = "iVBORw0KGgoAAAANSUhEUgAAAAQAAAAECAYAAACp8Z5+AAAAG0lEQVR4AYTIoQ0AAADCMLLLOR0/Q2WpEPkxAAAA//99eYBQAAAABklEQVQDAN2nCAkPbnNjAAAAAElFTkSuQmCC";
 
-async function waitForStudio(page: import("@playwright/test").Page): Promise<void> {
+async function waitForStudio(
+  page: import("@playwright/test").Page,
+  options: { requireWebGL?: boolean } = {},
+): Promise<"ready" | "restored" | "failed"> {
   await page.goto("/");
   await expect(page.locator(".asset-list li").first()).toBeVisible({ timeout: 30_000 });
-  await expect(page.locator(".stage-frame")).toHaveAttribute("data-context", /ready|restored/);
+  const frame = page.locator(".stage-frame");
+  await expect(frame).toHaveAttribute("data-context", /ready|restored|failed/, { timeout: 30_000 });
+  const state = await frame.getAttribute("data-context");
+  if (options.requireWebGL && state === "failed") {
+    test.skip(true, "This scenario requires the real WebGL evaluator; the DOM fallback was verified instead.");
+  }
+  return state as "ready" | "restored" | "failed";
 }
 
 test("boots WebGL2, exposes real controls, restores context, and fits phone viewports", async ({ page }) => {
@@ -20,7 +29,7 @@ test("boots WebGL2, exposes real controls, restores context, and fits phone view
     if (message.type() === "error") errors.push(message.text());
   });
 
-  await waitForStudio(page);
+  await waitForStudio(page, { requireWebGL: true });
   await expect(page.locator(".asset-list li")).toHaveCount(8);
   await expect(page.getByText(/WebGL2 · (H.264 ready|PNG output)/)).toBeVisible();
 
@@ -87,7 +96,7 @@ test("keyboard controls stay visible, file pickers stay out of Tab order, and sl
   await slideChooser.setFiles([]);
 
   const presenterChooserPromise = page.waitForEvent("filechooser");
-  await page.getByRole("button", { name: "Presenter", exact: true }).click();
+  await page.getByRole("button", { name: "Add presenter", exact: true }).click();
   const presenterChooser = await presenterChooserPromise;
   expect(presenterChooser.isMultiple()).toBe(false);
   await presenterChooser.setFiles([]);
@@ -237,7 +246,7 @@ test("saved starter studies remain replaceable by the first real deck", async ({
 });
 
 test("presenter playback obeys pause and export while removal preserves an unrelated slide pin", async ({ page }) => {
-  await waitForStudio(page);
+  await waitForStudio(page, { requireWebGL: true });
   const encodedPresenter = (await readFile(presenterFixturePath)).toString("base64");
   const playback = await page.evaluate(async (encoded) => {
     const [{ CinematicCarousel }, { DEFAULT_SETTINGS }] = await Promise.all([
@@ -521,13 +530,14 @@ test("WebGL2 denial yields an explicit, usable DOM fallback", async ({ page }) =
   await expect(page.getByText(/Cinematic export is blocked/)).toBeVisible();
   await expect(page.getByRole("button", { name: "Save portable project" })).toBeVisible();
   await expect(page.getByRole("button", { name: "Add slides" })).toBeVisible();
-  await page.getByRole("button", { name: "Export MP4 master" }).click();
-  await expect(page.getByRole("alert")).toContainText("Cinematic renderer is unavailable; export is blocked.");
+  await expect(page.getByRole("button", { name: "Export MP4 master" })).toBeDisabled();
+  await expect(page.getByRole("button", { name: "Save PNG poster" })).toBeDisabled();
+  await expect(page.getByRole("button", { name: "Export PNG sequence" })).toBeDisabled();
   expect(await page.evaluate(() => (window as Window & { __driftSavePickerCalls?: number }).__driftSavePickerCalls)).toBe(0);
 });
 
 test("export lifecycle preserves playback truth and releases a failed GPU preflight", async ({ page }) => {
-  await waitForStudio(page);
+  await waitForStudio(page, { requireWebGL: true });
   await page.getByLabel("Stage width").fill("256");
   await page.getByLabel("Stage height").fill("256");
   await page.getByRole("slider", { name: "Duration" }).fill("3");
@@ -536,7 +546,7 @@ test("export lifecycle preserves playback truth and releases a failed GPU prefli
   await expect(page.getByRole("button", { name: "Pause preview" })).toBeVisible();
 
   const stillDownload = page.waitForEvent("download");
-  await page.getByRole("button", { name: "Save transparent-safe PNG" }).click();
+  await page.getByRole("button", { name: "Save PNG poster" }).click();
   expect(await (await stillDownload).path()).toBeTruthy();
   const pause = page.getByRole("button", { name: "Pause preview" });
   await expect(pause).toBeEnabled();
@@ -578,7 +588,7 @@ test("export lifecycle preserves playback truth and releases a failed GPU prefli
     });
   });
 
-  await page.getByRole("button", { name: "Save transparent-safe PNG" }).click();
+  await page.getByRole("button", { name: "Save PNG poster" }).click();
   await expect(page.getByRole("alert")).toContainText("exceeds this GPU's safe WebGL limit of 128 × 128");
   const projectDownload = page.waitForEvent("download");
   await page.getByRole("button", { name: "Save portable project" }).click();
@@ -587,7 +597,7 @@ test("export lifecycle preserves playback truth and releases a failed GPU prefli
 });
 
 test("transparent PNG stores straight-alpha colour without dark fringes", async ({ page }) => {
-  await page.goto("/");
+  await waitForStudio(page, { requireWebGL: true });
   const receipt = await page.evaluate(async (encodedPng) => {
     const [{ CinematicCarousel }, { DEFAULT_SETTINGS }, { exportPngStill }] = await Promise.all([
       import("/src/engine/CinematicCarousel.ts"),
@@ -704,7 +714,7 @@ test("transparent PNG stores straight-alpha colour without dark fringes", async
 });
 
 test("scene-wide optics are deterministic while a protected presenter stays crisp", async ({ page }) => {
-  await page.goto("/");
+  await waitForStudio(page, { requireWebGL: true });
   const receipt = await page.evaluate(async () => {
     const [{ CinematicCarousel }, { DEFAULT_SETTINGS }] = await Promise.all([
       import("/src/engine/CinematicCarousel.ts"),
@@ -880,7 +890,7 @@ test("scene-wide optics are deterministic while a protected presenter stays cris
 });
 
 test("seamless lock closes the carousel, atmosphere, and lens on the same exact frame", async ({ page }) => {
-  await page.goto("/");
+  await waitForStudio(page, { requireWebGL: true });
   const receipt = await page.evaluate(async () => {
     const [{ CinematicCarousel }, { DEFAULT_SETTINGS }] = await Promise.all([
       import("/src/engine/CinematicCarousel.ts"),
@@ -1004,7 +1014,7 @@ test("seamless lock closes the carousel, atmosphere, and lens on the same exact
 });
 
 test("cover focal controls reach both source edges in both axes", async ({ page }) => {
-  await page.goto("/");
+  await waitForStudio(page, { requireWebGL: true });
   const samples = await page.evaluate(async () => {
     const [{ CinematicCarousel }, { DEFAULT_SETTINGS }, { exportPngStill }] = await Promise.all([
       import("/src/engine/CinematicCarousel.ts"),
@@ -1144,7 +1154,7 @@ test("cover focal controls reach both source edges in both axes", async ({ page
 });
 
 test("renderer pool and media replacement always preserve latest visual intent", async ({ page }) => {
-  await page.goto("/");
+  await waitForStudio(page, { requireWebGL: true });
   const receipt = await page.evaluate(async () => {
     const [{ CinematicCarousel }, { DEFAULT_SETTINGS }] = await Promise.all([
       import("/src/engine/CinematicCarousel.ts"),
@@ -1377,6 +1387,11 @@ test("renderer pool and media replacement always preserve latest visual intent",
 
 test("a pinned image outside the moving mesh pool is awaited before export", async ({ page }) => {
   await page.goto("/");
+  const frame = page.locator(".stage-frame");
+  await expect(frame).toHaveAttribute("data-context", /ready|restored|failed/, { timeout: 30_000 });
+  if (await frame.getAttribute("data-context") === "failed") {
+    test.skip(true, "This scenario requires the real WebGL evaluator; the DOM fallback was verified instead.");
+  }
   const receipt = await page.evaluate(async () => {
     const [{ CinematicCarousel }, { DEFAULT_SETTINGS }] = await Promise.all([
       import("/src/engine/CinematicCarousel.ts"),
diff --git a/e2e/visualEvidence.e2e.ts b/e2e/visualEvidence.e2e.ts
index 5a1841c5aa751d649bba27771d382e6a3e23c93e..7b422944fcaabf6477cacb08c3a620fa8152d9ed 100644
--- a/e2e/visualEvidence.e2e.ts
+++ b/e2e/visualEvidence.e2e.ts
@@ -41,7 +41,7 @@ test("retain visual evidence for the authored journey and publishing truth", asy
   await page.getByRole("combobox", { name: "Background", exact: true }).selectOption("transparent");
   await journey.getByRole("button", { name: /Master/i }).click();
   await expect(page.getByRole("button", { name: "Export MP4 master" })).toBeDisabled();
-  await expect(page.getByRole("button", { name: "Save transparent-safe PNG" })).toBeEnabled();
+  await expect(page.getByRole("button", { name: "Save PNG poster" })).toBeEnabled();
   await expect(page.getByRole("button", { name: "Export PNG sequence" })).toBeEnabled();
   await page.waitForTimeout(250);
   await page.screenshot({
diff --git a/e2e/worldExposureContract.e2e.ts b/e2e/worldExposureContract.e2e.ts
index 477408950e40780e8764e87bd2e23ed243c0a71f..fd6e1fafe8946e8fa83ce805391f49474444d8de 100644
--- a/e2e/worldExposureContract.e2e.ts
+++ b/e2e/worldExposureContract.e2e.ts
@@ -12,6 +12,12 @@ test("authored worlds retain image information instead of collapsing into blank
   await page.emulateMedia({ reducedMotion: "reduce" });
   await page.goto("/");
 
+  const frame = page.locator(".stage-frame");
+  await expect(frame).toHaveAttribute("data-context", /ready|restored|failed/, { timeout: 30_000 });
+  if (await frame.getAttribute("data-context") === "failed") {
+    test.skip(true, "This visual contract requires the real WebGL evaluator.");
+  }
+
   const worldSelect = page
     .locator("select")
     .filter({ has: page.locator("option", { hasText: "Editorial Drift" }) })
@@ -19,7 +25,7 @@ test("authored worlds retain image information instead of collapsing into blank
   await expect(worldSelect).toBeVisible();
 
   const pause = page.getByRole("button", { name: /pause preview/i });
-  if (await pause.isVisible().catch(() => false)) await pause.click();
+  if (await pause.isEnabled().catch(() => false)) await pause.click();
 
   const options = await worldSelect.locator("option").evaluateAll((nodes) =>
     nodes.map((node) => ({
diff --git a/e2e/worldVisualContract.e2e.ts b/e2e/worldVisualContract.e2e.ts
index 229f218e3d868745640cb6b1d8b695891b98ed55..44f25a56bb950201d796f022761d8ec26e442ba4 100644
--- a/e2e/worldVisualContract.e2e.ts
+++ b/e2e/worldVisualContract.e2e.ts
@@ -11,6 +11,12 @@ test("every authored film world produces a distinct rendered canvas", async ({ p
   await page.emulateMedia({ reducedMotion: "reduce" });
   await page.goto("/");
 
+  const frame = page.locator(".stage-frame");
+  await expect(frame).toHaveAttribute("data-context", /ready|restored|failed/, { timeout: 30_000 });
+  if (await frame.getAttribute("data-context") === "failed") {
+    test.skip(true, "This visual contract requires the real WebGL evaluator.");
+  }
+
   const worldSelect = page
     .locator("select")
     .filter({ has: page.locator("option", { hasText: "Editorial Drift" }) })
@@ -18,7 +24,7 @@ test("every authored film world produces a distinct rendered canvas", async ({ p
   await expect(worldSelect).toBeVisible();
 
   const pause = page.getByRole("button", { name: /pause preview/i });
-  if (await pause.isVisible().catch(() => false)) await pause.click();
+  if (await pause.isEnabled().catch(() => false)) await pause.click();
 
   const options = await worldSelect.locator("option").evaluateAll((nodes) =>
     nodes
diff --git a/src/App.tsx b/src/App.tsx
index 62d653260be27ed200bf964e46f917571c6c252f..19ff490ce3b745333a722ae9392b884ce145f544 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -273,12 +273,13 @@ export function App() {
   const [focusMode, setFocusMode] = useState(false);
   const [activePanel, setActivePanel] = useState<"media" | "stage" | "director">("stage");
   const [exportProgress, setExportProgress] = useState<ExportProgress | null>(null);
-  const [notice, setNotice] = useState<string | null>("Loading local studio…");
+  const [notice, setNotice] = useState<string | null>(null);
   const [noticeKind, setNoticeKind] = useState<"quiet" | "good" | "error">("quiet");
   const [saveState, setSaveState] = useState<"loading" | "saving" | "saved" | "failed" | "recovery">("loading");
   const [projectBusy, setProjectBusy] = useState(false);
   const [mp4Supported, setMp4Supported] = useState<boolean | null>(null);
   const [journeyStep, setJourneyStep] = useState<JourneyStepId>("slides");
+  const pendingJourneyStepRef = useRef<JourneyStepId | null>(null);
   const [guideMode, setGuideMode] = useState<CompositionGuideMode>("off");
   const [cleanLensComparison, setCleanLensComparison] = useState(false);
   const [posterPreview, setPosterPreview] = useState(false);
@@ -646,7 +647,6 @@ export function App() {
           }
           assetsRef.current = demo;
           setAssets(demo);
-          announce("A live study is loaded. Replace it with your own deck whenever you’re ready.");
         }
         hydrationSucceeded = true;
         recoverySnapshotRef.current = null;
@@ -852,16 +852,80 @@ export function App() {
   }, [exitPosterPreview, posterPreview]);
 
   const handleJourneySelect = useCallback((step: JourneyStepId) => {
+    pendingJourneyStepRef.current = step;
     setJourneyStep(step);
-    const panel = step === "slides" ? "media" : "director";
-    setActivePanel(panel);
-    window.requestAnimationFrame(() => {
+    setActivePanel(step === "slides" ? "media" : "director");
+  }, []);
+
+  useEffect(() => {
+    const step = pendingJourneyStepRef.current;
+    if (!step) return;
+    const expectedPanel = step === "slides" ? "media" : "director";
+    if (activePanel !== expectedPanel || journeyStep !== step) return;
+
+    let cancelled = false;
+    let secondFrame = 0;
+    let settleTimer = 0;
+    let observer: ResizeObserver | null = null;
+
+    const alignDestination = (allowSmooth: boolean, focus: boolean): boolean => {
+      if (cancelled || pendingJourneyStepRef.current !== step) return false;
       const destination = document.querySelector<HTMLElement>(`[data-journey-section="${step}"]`);
-      const behavior: ScrollBehavior = window.matchMedia("(prefers-reduced-motion: reduce)").matches ? "auto" : "smooth";
-      destination?.scrollIntoView({ behavior, block: "start" });
-      if (step !== "slides") destination?.focus({ preventScroll: true });
+      const scroller = destination?.closest<HTMLElement>(step === "slides" ? ".media-library" : ".inspector");
+      if (!destination || !scroller || scroller.clientHeight <= 0) return false;
+
+      const scrollerRect = scroller.getBoundingClientRect();
+      const destinationRect = destination.getBoundingClientRect();
+      const targetTop = Math.max(0, scroller.scrollTop + destinationRect.top - scrollerRect.top - 8);
+      const distance = Math.abs(targetTop - scroller.scrollTop);
+      const compact = window.matchMedia("(max-width: 1120px)").matches;
+      const reduced = window.matchMedia("(prefers-reduced-motion: reduce)").matches;
+      const behavior: ScrollBehavior = allowSmooth && !compact && !reduced && distance <= scroller.clientHeight * 1.25
+        ? "smooth"
+        : "auto";
+
+      scroller.scrollTo({ top: targetTop, behavior });
+      if (focus && step !== "slides") destination.focus({ preventScroll: true });
+      return true;
+    };
+
+    const firstFrame = window.requestAnimationFrame(() => {
+      // The mobile panel changes from display:none to display:block in the same
+      // commit as the journey selection. A second frame guarantees measurable
+      // geometry before the first alignment.
+      secondFrame = window.requestAnimationFrame(() => {
+        if (!alignDestination(true, true)) return;
+
+        // Project hydration, codec probing, and lazy renderer state can change
+        // the height of sections above the destination after the initial tap.
+        // Observe those authored sections briefly and keep the requested step
+        // anchored instead of abandoning the user halfway through Lens controls.
+        if (typeof ResizeObserver !== "undefined") {
+          const destination = document.querySelector<HTMLElement>(`[data-journey-section="${step}"]`);
+          const scroller = destination?.closest<HTMLElement>(step === "slides" ? ".media-library" : ".inspector");
+          if (scroller) {
+            observer = new ResizeObserver(() => { alignDestination(false, false); });
+            scroller.querySelectorAll<HTMLElement>("[data-journey-section]").forEach((section) => observer?.observe(section));
+          }
+        }
+
+        settleTimer = window.setTimeout(() => {
+          alignDestination(false, false);
+          observer?.disconnect();
+          observer = null;
+          if (pendingJourneyStepRef.current === step) pendingJourneyStepRef.current = null;
+        }, 1_200);
+      });
     });
-  }, []);
+
+    return () => {
+      cancelled = true;
+      window.cancelAnimationFrame(firstFrame);
+      if (secondFrame) window.cancelAnimationFrame(secondFrame);
+      if (settleTimer) window.clearTimeout(settleTimer);
+      observer?.disconnect();
+    };
+  }, [activePanel, journeyStep]);
 
   const matchSlideFrame = useCallback(() => {
     if (deckHealth.status === "empty" || deckHealth.mixedRatios) return;
diff --git a/src/components/ControlPanel.tsx b/src/components/ControlPanel.tsx
index 3b5eab38fd38b8f5dab5c425f334d5601182a608..3e881e272fdc03517d32cf8d480c22e370e0c1b3 100644
--- a/src/components/ControlPanel.tsx
+++ b/src/components/ControlPanel.tsx
@@ -248,7 +248,7 @@ export function ControlPanel({
           </button>
           <button type="button" data-active={posterPreview} onClick={onTogglePosterPreview} disabled={exporting || !rendererAvailable} aria-pressed={posterPreview}>
             <span>{posterPreview ? "EXIT POSTER PREVIEW" : `PREVIEW POSTER · ${settings.output.posterTime.toFixed(1)} S`}</span>
-            <small>{rendererAvailable ? "Judge the exact frame used by transparent-safe PNG export." : "Requires the cinematic evaluator; poster export remains blocked in fallback mode."}</small>
+            <small>{rendererAvailable ? "Judge the exact frame used by PNG poster export." : "Requires the cinematic evaluator; poster export remains blocked in fallback mode."}</small>
           </button>
         </div>
 
@@ -417,7 +417,7 @@ export function ControlPanel({
           <Segmented label="Frame rate" value={settings.output.fps} options={[{ value: 24 as const, label: "24" }, { value: 25 as const, label: "25" }, { value: 30 as const, label: "30" }, { value: 50 as const, label: "50" }, { value: 60 as const, label: "60" }]} onChange={(fps) => patch("output", { fps })} />
           <div className="master-status-line" data-status={masterStatus}>
             <span>{readinessLabel(masterStatus)} · {availablePublishingPaths}/3 PUBLISHING PATHS</span>
-            <small>A blocked codec never invalidates a valid transparent PNG path.</small>
+            <small>Each publishing path is checked independently. A blocked codec never invalidates a valid PNG.</small>
           </div>
           <div className="destination-grid" aria-label="Publishing destinations">
             <article data-status={mp4Readiness.status}>
@@ -426,9 +426,9 @@ export function ControlPanel({
               <small>{mp4Readiness.blockingReason ?? `${(settings.output.videoBitrate / 1_000_000).toFixed(0)} Mbit/s H.264 · deterministic picture${settings.presenter.enabled && !settings.presenter.muted ? " + verified AAC" : ""}.`}</small>
             </article>
             <article data-status={stillReadiness.status}>
-              <span>PNG STILL</span>
+              <span>PNG POSTER</span>
               <strong>{readinessLabel(stillReadiness.status)} · {settings.output.posterTime.toFixed(1)} S</strong>
-              <small>{stillReadiness.blockingReason ?? "One exact poster frame with an alpha-capable channel."}</small>
+              <small>{stillReadiness.blockingReason ?? "One exact poster frame. Transparency is preserved when the scene uses it."}</small>
             </article>
             <article data-status={sequenceReadiness.status}>
               <span>PNG SEQUENCE</span>
@@ -438,7 +438,7 @@ export function ControlPanel({
           </div>
           <div className="action-stack">
             <button type="button" className="primary-action" onClick={onExportVideo} disabled={exporting || mp4Readiness.status === "blocked" || mp4Readiness.status === "checking"}>Export MP4 master</button>
-            <button type="button" onClick={onExportStill} disabled={exporting || stillReadiness.status === "blocked" || stillReadiness.status === "checking"}>Save transparent-safe PNG</button>
+            <button type="button" onClick={onExportStill} disabled={exporting || stillReadiness.status === "blocked" || stillReadiness.status === "checking"}>Save PNG poster</button>
             <button type="button" onClick={onExportFrames} disabled={exporting || sequenceReadiness.status === "blocked" || sequenceReadiness.status === "checking"}>Export PNG sequence</button>
           </div>
           <div className="project-actions">
diff --git a/src/components/MediaLibrary.tsx b/src/components/MediaLibrary.tsx
index 715672e5ab11246e421193d2b0492ee177bb347f..5d0d43ff6cadc39ad4e0380c5f9e169ce70e6ff1 100644
--- a/src/components/MediaLibrary.tsx
+++ b/src/components/MediaLibrary.tsx
@@ -92,7 +92,7 @@ export function MediaLibrary({
           <span aria-hidden="true">＋</span> Add slides
         </button>
         <button type="button" className="media-add subtle" disabled={busy} onClick={() => presenterInput.current?.click()}>
-          Presenter
+          <span aria-hidden="true">＋</span> Add presenter
         </button>
       </div>
       <p className="media-note">{demoOnly ? "The first import replaces this study. Nothing uploads." : "Images move. One optional video can stay pinned. Files remain on this device."}</p>
diff --git a/src/components/Stage.tsx b/src/components/Stage.tsx
index bde291eff7cd8825adcaa9ff5862084cfe685d26..7a9e9d68640f45315e2f19f1c52377ac7648768f 100644
--- a/src/components/Stage.tsx
+++ b/src/components/Stage.tsx
@@ -124,9 +124,17 @@ export function Stage({
               <span>DOM FALLBACK</span>
               <strong>Cinematic renderer unavailable.</strong>
               <p>{webglError}</p>
-              <div className="fallback-strip">
-                {assets.map((asset) => <figure key={asset.id}><img src={asset.objectUrl} alt={asset.name} /><figcaption>{asset.name}</figcaption></figure>)}
-              </div>
+              {assets.length === 0 ? (
+                <div className="fallback-empty">
+                  <span>DROP SLIDES</span>
+                  <strong>A film needs frames.</strong>
+                  <small>PNG · JPEG · WebP · AVIF</small>
+                </div>
+              ) : (
+                <div className="fallback-strip">
+                  {assets.map((asset) => <figure key={asset.id}><img src={asset.objectUrl} alt={asset.name} /><figcaption>{asset.name}</figcaption></figure>)}
+                </div>
+              )}
               <small>Media and project controls remain usable. Cinematic export is blocked, never silently downgraded.</small>
             </div>
           ) : null}
diff --git a/src/engine/shaders.ts b/src/engine/shaders.ts
index e24f2431addb53a51fbc4881d4140c2982ac7b6d..170d5ac4f1f3d4bebe16e9edf4ebe9bb53ac049f 100644
--- a/src/engine/shaders.ts
+++ b/src/engine/shaders.ts
@@ -42,6 +42,7 @@ export const slideFragmentShader = /* glsl */ `
   uniform float uDistortion;
   uniform float uAxis;
   uniform float uTime;
+  uniform float uPhase;
 
   float roundedSuperellipseDistance(vec2 p, vec2 halfSize, float radius, float smoothing) {
     if (radius < 0.5) {
@@ -108,7 +109,11 @@ export const slideFragmentShader = /* glsl */ `
       sampled.a = 1.0;
     }
 
-    float grain = (hash12(gl_FragCoord.xy + fract(uTime) * 113.0) - 0.5) * 0.018;
+    // Slide microtexture belongs to the photographed surface, not the wall clock.
+    // A time-driven seed makes paused frames shimmer and prevents an honest
+    // clean-lens comparison from restoring the exact directed image.
+    vec2 grainSeed = vec2(uPhase * 19.37, uPhase * -11.13);
+    float grain = (hash12(gl_FragCoord.xy + grainSeed) - 0.5) * 0.018;
     sampled.rgb += grain + abs(vWarp) * 0.025;
     vec3 color = mix(sampled.rgb, uBorderColor, borderMask * uBorderOpacity);
     float alpha = outerMask * sampled.a * uOpacity;
diff --git a/src/lib/directorCommands.ts b/src/lib/directorCommands.ts
index 6d799b6a37a1bb804cb3d58a7151975141aa79bb..dd4614c57e2b7e4568bd0db100dfdd9aa5b0f281 100644
--- a/src/lib/directorCommands.ts
+++ b/src/lib/directorCommands.ts
@@ -212,7 +212,7 @@ function installStyles(): void {
   const style = document.createElement("style");
   style.id = STYLE_ID;
   style.textContent = `
-    .director-command-launcher { position: fixed; right: max(16px, env(safe-area-inset-right)); bottom: max(16px, env(safe-area-inset-bottom)); z-index: 9000; display: inline-flex; align-items: center; gap: 9px; min-height: 34px; padding: 0 11px; border: 1px solid rgba(255,255,255,.13); border-radius: 999px; background: rgba(16,16,19,.78); color: rgba(245,241,232,.76); box-shadow: 0 12px 36px rgba(0,0,0,.24); backdrop-filter: blur(14px); font: 650 9px/1 ui-monospace, SFMono-Regular, Menlo, monospace; letter-spacing: .08em; text-transform: uppercase; cursor: pointer; }
+    .director-command-launcher { position: fixed; right: max(16px, env(safe-area-inset-right)); bottom: max(34px, calc(env(safe-area-inset-bottom) + 34px)); z-index: 9000; display: inline-flex; align-items: center; gap: 9px; min-height: 34px; padding: 0 11px; border: 1px solid rgba(255,255,255,.13); border-radius: 999px; background: rgba(16,16,19,.78); color: rgba(245,241,232,.76); box-shadow: 0 12px 36px rgba(0,0,0,.24); backdrop-filter: blur(14px); font: 650 9px/1 ui-monospace, SFMono-Regular, Menlo, monospace; letter-spacing: .08em; text-transform: uppercase; cursor: pointer; }
     .director-command-launcher:hover, .director-command-launcher:focus-visible { color: #fffaf0; border-color: rgba(255,255,255,.27); outline: none; }
     .director-command-launcher kbd { padding: 4px 6px; border: 1px solid rgba(255,255,255,.13); border-radius: 7px; background: rgba(255,255,255,.055); font: inherit; }
     [data-director-target='true'] { outline: 2px solid rgba(255,235,194,.92) !important; outline-offset: 5px !important; }
@@ -235,7 +235,7 @@ function installStyles(): void {
     .director-command-section, .director-command-key { border: 1px solid rgba(255,255,255,.12); border-radius: 999px; padding: 5px 7px; font: 600 9px/1 ui-monospace,SFMono-Regular,Menlo,monospace; letter-spacing: .06em; text-transform: uppercase; opacity: .58; white-space: nowrap; }
     .director-command-empty { padding: 34px 18px; text-align: center; color: rgba(245,241,232,.56); font: 500 12px/1.5 system-ui,sans-serif; }
     .director-command-foot { display: flex; justify-content: space-between; gap: 12px; padding: 11px 18px 13px; border-top: 1px solid rgba(255,255,255,.08); color: rgba(245,241,232,.46); font: 500 9px/1.35 ui-monospace,SFMono-Regular,Menlo,monospace; letter-spacing: .06em; text-transform: uppercase; }
-    @media (max-width: 620px) { .director-command-root { padding-top: 18px; } .director-command-dialog { border-radius: 20px; } .director-command-path { grid-template-columns: repeat(2,1fr); } .director-command-meta { display: none; } .director-command-launcher span { display: none; } }
+    @media (max-width: 620px) { .director-command-root { padding-top: 18px; } .director-command-dialog { border-radius: 20px; } .director-command-path { grid-template-columns: repeat(2,1fr); } .director-command-meta { display: none; } .director-command-launcher { right: max(12px, env(safe-area-inset-right)); bottom: max(34px, calc(env(safe-area-inset-bottom) + 34px)); width: 38px; min-height: 38px; justify-content: center; padding: 0; } .director-command-launcher span, .director-command-launcher kbd { display: none; } .director-command-launcher::before { content: attr(data-compact-key); font-size: 11px; letter-spacing: 0; } body:has(.app[data-active-panel='stage']) .director-command-launcher { bottom: max(74px, calc(env(safe-area-inset-bottom) + 74px)); } }
     @media (prefers-reduced-motion: reduce) { .director-command-root, .director-command-launcher { backdrop-filter: none; } }
   `;
   document.head.append(style);
@@ -244,6 +244,8 @@ function installStyles(): void {
 export function installDirectorCommands(): () => void {
   if (document.getElementById(ROOT_ID)) return () => undefined;
   installStyles();
+  const shortcutLabel = /Mac|iPhone|iPad|iPod/i.test(navigator.platform) ? "⌘K" : "Ctrl K";
+  const compactKey = shortcutLabel === "⌘K" ? "⌘" : "K";
   const root = document.createElement("div");
   root.id = ROOT_ID;
   root.className = "director-command-root";
@@ -256,14 +258,16 @@ export function installDirectorCommands(): () => void {
       </header>
       <div class="director-command-path" aria-label="Recommended workflow"><span>1 · Slides</span><span>2 · World</span><span>3 · Direct</span><span>4 · Review</span><span>5 · Master</span></div>
       <div id="director-command-list" class="director-command-list" role="listbox" aria-label="Commands"></div>
-      <footer class="director-command-foot"><span>↑ ↓ choose · ↵ run</span><span>Esc closes · ⌘K opens</span></footer>
+      <footer class="director-command-foot"><span>↑ ↓ choose · ↵ run</span><span>Esc closes · ${shortcutLabel} opens</span></footer>
     </section>`;
   const launcher = document.createElement("button");
   launcher.type = "button";
   launcher.className = "director-command-launcher";
   launcher.setAttribute("aria-haspopup", "dialog");
   launcher.setAttribute("aria-expanded", "false");
-  launcher.innerHTML = `<span>Commands</span><kbd>⌘K</kbd>`;
+  launcher.setAttribute("aria-label", `Open Director Commands (${shortcutLabel})`);
+  launcher.dataset.compactKey = compactKey;
+  launcher.innerHTML = `<span>Commands</span><kbd>${shortcutLabel}</kbd>`;
   const live = document.createElement("div");
   live.className = "director-command-live";
   live.setAttribute("role", "status");
diff --git a/src/styles.css b/src/styles.css
index 2bfa20d48ed1ecb5f6346e376e211d03090f2afe..40ca96f4ecd40fd263c1c2de397c112d45d30b72 100644
--- a/src/styles.css
+++ b/src/styles.css
@@ -1393,7 +1393,13 @@ a:focus-visible {
   .transport-copy,
   .transport-divider { display: none; }
   .transport .focus-button { padding: 0 3px; }
-  .notice { bottom: 31px; }
+  .notice {
+    right: 62px;
+    bottom: max(34px, calc(env(safe-area-inset-bottom) + 34px));
+    left: 12px;
+    max-width: none;
+    transform: none;
+  }
   .app-footer { min-width: 0; justify-content: center; gap: 12px; white-space: nowrap; }
   .app-footer > span:first-child { min-width: 0; overflow: hidden; text-overflow: ellipsis; }
   .app-footer .legal-notice { flex: 0 0 auto; }
@@ -2217,3 +2223,31 @@ a:focus-visible {
 @media (prefers-reduced-motion: reduce) {
   .renderer-loading-line::after { animation: none; transform: translateX(58%); }
 }
+
+.fallback-empty {
+  display: grid;
+  justify-items: center;
+  gap: 8px;
+  margin: 18px auto;
+  padding: 18px 20px;
+  max-width: 320px;
+  border: 1px solid color-mix(in srgb, var(--line) 82%, transparent);
+  background: color-mix(in srgb, var(--panel) 88%, transparent);
+  text-align: center;
+}
+
+.fallback-empty > span,
+.fallback-empty > small {
+  font-family: var(--mono);
+  font-size: 10px;
+  letter-spacing: 0.12em;
+  text-transform: uppercase;
+  color: var(--muted);
+}
+
+.fallback-empty > strong {
+  font-family: var(--serif);
+  font-size: clamp(20px, 2vw, 30px);
+  font-weight: 400;
+  color: var(--ink);
+}
diff --git a/tests/creatorSurface.test.ts b/tests/creatorSurface.test.ts
index 8d312f12aae774f0f3b6513492b58962d948ad33..b8ceafd94ac61eff95d9ef6aa23f6ec5a903b01f 100644
--- a/tests/creatorSurface.test.ts
+++ b/tests/creatorSurface.test.ts
@@ -1,4 +1,4 @@
-import { readdirSync, readFileSync } from "node:fs";
+import { existsSync, readdirSync, readFileSync } from "node:fs";
 import { extname, join, relative } from "node:path";
 import { fileURLToPath } from "node:url";
 import { describe, expect, it } from "vitest";
@@ -55,11 +55,28 @@ describe("creator-surface truth", () => {
   });
 
   it("keeps temporary code-delivery machinery out of the review tree", () => {
-    const workflowRoot = join(root, ".github", "workflows");
+    const githubRoot = join(root, ".github");
+    const workflowRoot = join(githubRoot, "workflows");
     const workflows = readdirSync(workflowRoot).sort();
-    const temporary = workflows.filter((name) =>
-      /snapshot|gauntlet|take-shelf|harden-director|direction-packs|diagnostics|world-audition|visual-atlas/i.test(name),
-    );
-    expect(temporary).toEqual([]);
+    expect(workflows).toEqual(["ci.yml"]);
+    expect(existsSync(join(githubRoot, "creator-journey-payload"))).toBe(false);
+
+    const workflow = readFileSync(join(workflowRoot, "ci.yml"), "utf8");
+    expect(workflow).not.toMatch(/contents:\s*write/);
+    expect(workflow).not.toMatch(/git\s+push/);
+    expect(workflow).not.toMatch(/base64\s+--decode|patch\s+-p1/);
+  });
+
+  it("uses creator language without a redundant first-load toast", () => {
+    const app = source.get("App.tsx") ?? "";
+    const controls = source.get("components/ControlPanel.tsx") ?? "";
+    const media = source.get("components/MediaLibrary.tsx") ?? "";
+
+    expect(app).not.toContain("A live study is loaded");
+    expect(app).not.toContain("Loading local studio");
+    expect(controls).toContain("PNG POSTER");
+    expect(controls).toContain("Save PNG poster");
+    expect(controls).not.toContain("transparent-safe PNG");
+    expect(media).toContain("Add presenter");
   });
 });
diff --git a/tests/engineShader.test.ts b/tests/engineShader.test.ts
index 71e1a2c6ad3c25b1ddf00589d01e59ef004fdb57..7eeed886023d4e98877167f5f1a421ce4aceda6d 100644
--- a/tests/engineShader.test.ts
+++ b/tests/engineShader.test.ts
@@ -37,6 +37,13 @@ describe("custom shader output contract", () => {
     expect(slideFragmentShader).toContain("scale.y = planeAspect / imageAspect");
     expect(slideFragmentShader).toContain("scale.x = imageAspect / planeAspect");
   });
+
+  it("pins slide microtexture to the slide instead of the wall clock", () => {
+    expect(slideFragmentShader).toContain("uniform float uPhase;");
+    expect(slideFragmentShader).toContain("grainSeed");
+    expect(slideFragmentShader).toContain("uPhase * 19.37");
+    expect(slideFragmentShader).not.toContain("fract(uTime)");
+  });
 });
 
 describe("export surface preflight", () => {
