from __future__ import annotations

from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]


def write(relative: str, content: str) -> None:
    path = ROOT / relative
    path.parent.mkdir(parents=True, exist_ok=True)
    path.write_text(content, encoding="utf-8")


def replace_exact(relative: str, old: str, new: str, label: str) -> None:
    path = ROOT / relative
    source = path.read_text(encoding="utf-8")
    count = source.count(old)
    if count != 1:
        raise RuntimeError(f"Expected one {label} in {relative}; found {count}")
    path.write_text(source.replace(old, new, 1), encoding="utf-8")


write("src/model.ts", "export const SCHEMA_VERSION = 1 as const;\nexport const ENGINE_VERSION = \"1.0.0\";\nexport const SHADER_VERSION = \"1.0.0\";\nexport const THEME_VERSION = \"1.0.0\";\nexport const LIGHTING_VERSION = 2 as const;\n\nexport type Axis = \"horizontal\" | \"vertical\";\nexport type Direction = 1 | -1;\nexport type Flow = \"straight\" | \"arc\" | \"ribbon\" | \"cylinder\" | \"tunnel\";\nexport type ImageFit = \"cover\" | \"contain\";\nexport type BackgroundStyle = \"transparent\" | \"solid\" | \"gradient\" | \"aura\" | \"paper\" | \"void\";\nexport type ThemeId = \"editorial-drift\" | \"road-memory\" | \"dread\" | \"noir-contact\" | \"tender-light\" | \"chrome-dream\";\nexport type LightingPresetId =\n  | \"custom\"\n  | \"studio-soft\"\n  | \"window-rake\"\n  | \"projector-haze\"\n  | \"noir-slice\"\n  | \"golden-hour\"\n  | \"electric-rim\"\n  | \"overcast-window\"\n  | \"moon-pool\"\n  | \"sodium-vapor\"\n  | \"lantern-flicker\"\n  | \"fluorescent-flat\"\n  | \"headlight-sweep\";\nexport type LightGobo =\n  | \"softbox\"\n  | \"window\"\n  | \"projector\"\n  | \"slit\"\n  | \"sunset\"\n  | \"edge\"\n  | \"overcast\"\n  | \"moon\"\n  | \"sodium\"\n  | \"lantern\"\n  | \"ceiling\"\n  | \"headlights\";\nexport type LightingMotion = \"static\" | \"breathe\" | \"sweep\" | \"flicker\" | \"orbit\";\nexport type LightingSpace = \"stage\" | \"card\";\n\nexport interface StageSettings {\n  width: number;\n  height: number;\n  transparent: boolean;\n}\n\nexport interface MotionSettings {\n  axis: Axis;\n  direction: Direction;\n  autoplay: boolean;\n  speed: number;\n  flow: Flow;\n  gap: number;\n  curvature: number;\n  depth: number;\n  tilt: number;\n  distortion: number;\n  focusScale: number;\n  edgeFade: number;\n  dragSensitivity: number;\n  seamless: boolean;\n  seamlessLoops: number;\n  reducedMotionOutput: boolean;\n}\n\nexport interface SlideSettings {\n  aspectWidth: number;\n  aspectHeight: number;\n  scale: number;\n  fit: ImageFit;\n  focalX: number;\n  focalY: number;\n  radius: number;\n  smoothing: number;\n  borderWidth: number;\n  borderColor: string;\n  borderOpacity: number;\n  shadowOpacity: number;\n  shadowSoftness: number;\n}\n\nexport interface BackgroundSettings {\n  style: BackgroundStyle;\n  colorA: string;\n  colorB: string;\n  accent: string;\n  intensity: number;\n  motion: number;\n  grain: number;\n  vignette: number;\n  seed: number;\n}\n\nexport interface LightingSettings {\n  version: typeof LIGHTING_VERSION;\n  preset: LightingPresetId;\n  enabled: boolean;\n  space: LightingSpace;\n  motionMode: LightingMotion;\n  motionSpeed: 1 | 2 | 3 | 4;\n  keyColor: string;\n  fillColor: string;\n  shadowColor: string;\n  azimuth: number;\n  elevation: number;\n  keyIntensity: number;\n  fillIntensity: number;\n  rimIntensity: number;\n  sheen: number;\n  roughness: number;\n  artworkProtection: number;\n  heroProtection: number;\n  shadowOpacity: number;\n  shadowSoftness: number;\n  shadowDistance: number;\n  contactStrength: number;\n  backgroundSpill: number;\n  spillFocus: number;\n  goboStrength: number;\n  breath: number;\n  gobo: LightGobo;\n}\n\nexport interface PresenterSettings {\n  enabled: boolean;\n  assetId: string | null;\n  x: number;\n  y: number;\n  width: number;\n  aspectWidth: number;\n  aspectHeight: number;\n  fit: ImageFit;\n  radius: number;\n  smoothing: number;\n  borderWidth: number;\n  borderColor: string;\n  borderOpacity: number;\n  shadowOpacity: number;\n  muted: boolean;\n  gain: number;\n  trimStart: number;\n  startAt: number;\n}\n\nexport interface OutputSettings {\n  width: number;\n  height: number;\n  fps: 24 | 25 | 30 | 50 | 60;\n  duration: number;\n  videoBitrate: number;\n  audioBitrate: number;\n}\n\nexport interface StudioSettings {\n  schemaVersion: typeof SCHEMA_VERSION;\n  engineVersion: string;\n  shaderVersion: string;\n  themeId: ThemeId;\n  stage: StageSettings;\n  motion: MotionSettings;\n  slide: SlideSettings;\n  background: BackgroundSettings;\n  lighting: LightingSettings;\n  presenter: PresenterSettings;\n  output: OutputSettings;\n}\n\nexport interface StudioAsset {\n  id: string;\n  name: string;\n  kind: \"image\" | \"video\";\n  blob: Blob;\n  mimeType: string;\n  width: number;\n  height: number;\n  duration?: number;\n  hash?: string;\n  objectUrl: string;\n  demo?: boolean;\n}\n\nexport interface StoredAssetDescriptor {\n  id: string;\n  name: string;\n  kind: \"image\" | \"video\";\n  mimeType: string;\n  width: number;\n  height: number;\n  duration?: number;\n  hash: string;\n  demo?: boolean;\n}\n\nexport interface ExportProgress {\n  phase: \"preparing\" | \"audio\" | \"video\" | \"frames\" | \"finalizing\" | \"complete\";\n  completed: number;\n  total: number;\n  message: string;\n}\n\nexport const DEFAULT_SETTINGS: StudioSettings = {\n  schemaVersion: SCHEMA_VERSION,\n  engineVersion: ENGINE_VERSION,\n  shaderVersion: SHADER_VERSION,\n  themeId: \"editorial-drift\",\n  stage: {\n    width: 1080,\n    height: 1920,\n    transparent: false,\n  },\n  motion: {\n    axis: \"vertical\",\n    direction: -1,\n    autoplay: true,\n    speed: 0.34,\n    flow: \"ribbon\",\n    gap: 0.22,\n    curvature: 0.36,\n    depth: 0.18,\n    tilt: 4.5,\n    distortion: 0.32,\n    focusScale: 0.08,\n    edgeFade: 0.28,\n    dragSensitivity: 1,\n    seamless: false,\n    seamlessLoops: 1,\n    reducedMotionOutput: false,\n  },\n  slide: {\n    aspectWidth: 16,\n    aspectHeight: 9,\n    scale: 0.78,\n    fit: \"cover\",\n    focalX: 0.5,\n    focalY: 0.5,\n    radius: 36,\n    smoothing: 0.6,\n    borderWidth: 1.5,\n    borderColor: \"#f0e6d4\",\n    borderOpacity: 0.42,\n    shadowOpacity: 0.34,\n    shadowSoftness: 34,\n  },\n  background: {\n    style: \"aura\",\n    colorA: \"#120f0c\",\n    colorB: \"#2c1516\",\n    accent: \"#c26d3f\",\n    intensity: 0.72,\n    motion: 0.34,\n    grain: 0.12,\n    vignette: 0.48,\n    seed: 17,\n  },\n  lighting: {\n    version: LIGHTING_VERSION,\n    preset: \"studio-soft\",\n    enabled: true,\n    space: \"stage\",\n    motionMode: \"breathe\",\n    motionSpeed: 1,\n    keyColor: \"#fff1dc\",\n    fillColor: \"#b9c9e8\",\n    shadowColor: \"#100c12\",\n    azimuth: 42,\n    elevation: 56,\n    keyIntensity: 0.78,\n    fillIntensity: 0.54,\n    rimIntensity: 0.14,\n    sheen: 0.16,\n    roughness: 0.72,\n    artworkProtection: 0.82,\n    heroProtection: 0.82,\n    shadowOpacity: 0.34,\n    shadowSoftness: 52,\n    shadowDistance: 54,\n    contactStrength: 0.58,\n    backgroundSpill: 0.28,\n    spillFocus: 0.78,\n    goboStrength: 0.08,\n    breath: 0.1,\n    gobo: \"softbox\",\n  },\n  presenter: {\n    enabled: false,\n    assetId: null,\n    x: 0.72,\n    y: 0.28,\n    width: 0.34,\n    aspectWidth: 9,\n    aspectHeight: 16,\n    fit: \"cover\",\n    radius: 42,\n    smoothing: 0.6,\n    borderWidth: 2,\n    borderColor: \"#f4ead8\",\n    borderOpacity: 0.55,\n    shadowOpacity: 0.48,\n    muted: false,\n    gain: 1,\n    trimStart: 0,\n    startAt: 0,\n  },\n  output: {\n    width: 1080,\n    height: 1920,\n    fps: 30,\n    duration: 8,\n    videoBitrate: 16_000_000,\n    audioBitrate: 192_000,\n  },\n};\n\nexport function cloneSettings(settings: StudioSettings): StudioSettings {\n  return structuredClone(settings);\n}\n\nexport function clearPinnedAssetIfRemoved(\n  settings: StudioSettings,\n  removedAssetId: string | null,\n): StudioSettings {\n  if (!removedAssetId || settings.presenter.assetId !== removedAssetId) return settings;\n  return {\n    ...settings,\n    presenter: { ...settings.presenter, enabled: false, assetId: null },\n  };\n}\n\nexport function stageAspect(settings: Pick<StudioSettings, \"stage\">): number {\n  return settings.stage.width / settings.stage.height;\n}\n\nexport function slideAspect(settings: Pick<StudioSettings, \"slide\">): number {\n  return settings.slide.aspectWidth / settings.slide.aspectHeight;\n}\n")
write("src/lighting.ts", "import {\n  LIGHTING_VERSION,\n  type LightGobo,\n  type LightingMotion,\n  type LightingPresetId,\n  type LightingSettings,\n} from \"./model\";\n\nexport type AuthoredLightingPresetId = Exclude<LightingPresetId, \"custom\">;\n\nexport interface LightingPreset {\n  id: AuthoredLightingPresetId;\n  name: string;\n  eyebrow: string;\n  description: string;\n  bestFor: string;\n  lighting: LightingSettings;\n}\n\ntype PresetLighting = Omit<LightingSettings, \"version\" | \"preset\">;\n\nfunction definePreset(\n  id: AuthoredLightingPresetId,\n  name: string,\n  eyebrow: string,\n  description: string,\n  bestFor: string,\n  lighting: PresetLighting,\n): LightingPreset {\n  return {\n    id,\n    name,\n    eyebrow,\n    description,\n    bestFor,\n    lighting: {\n      version: LIGHTING_VERSION,\n      preset: id,\n      ...lighting,\n    },\n  };\n}\n\nexport const LIGHTING_PRESETS: readonly LightingPreset[] = [\n  definePreset(\n    \"studio-soft\",\n    \"Studio Soft\",\n    \"Large source \u00b7 faithful colour\",\n    \"A broad warm key, quiet cool fill, and short contact-rich shadow. It gives the deck physical presence without re-grading it.\",\n    \"Dialogue, editorial, founder-led work, dense typography\",\n    {\n      enabled: true,\n      space: \"stage\",\n      motionMode: \"breathe\",\n      motionSpeed: 1,\n      keyColor: \"#fff1dc\",\n      fillColor: \"#b9c9e8\",\n      shadowColor: \"#100c12\",\n      azimuth: 42,\n      elevation: 56,\n      keyIntensity: 0.78,\n      fillIntensity: 0.54,\n      rimIntensity: 0.14,\n      sheen: 0.16,\n      roughness: 0.72,\n      artworkProtection: 0.82,\n      heroProtection: 0.82,\n      shadowOpacity: 0.34,\n      shadowSoftness: 52,\n      shadowDistance: 54,\n      contactStrength: 0.58,\n      backgroundSpill: 0.28,\n      spillFocus: 0.78,\n      goboStrength: 0.08,\n      breath: 0.1,\n      gobo: \"softbox\",\n    },\n  ),\n  definePreset(\n    \"window-rake\",\n    \"Window Rake\",\n    \"Low side key \u00b7 long afternoon\",\n    \"Warm directional light crosses the stage like a nearby window. A cool fill keeps faces, titles, and detail readable.\",\n    \"Travel, memory, domestic drama, warm documentary\",\n    {\n      enabled: true,\n      space: \"stage\",\n      motionMode: \"sweep\",\n      motionSpeed: 1,\n      keyColor: \"#ffd39b\",\n      fillColor: \"#8fa8d8\",\n      shadowColor: \"#160e12\",\n      azimuth: 142,\n      elevation: 28,\n      keyIntensity: 1.08,\n      fillIntensity: 0.3,\n      rimIntensity: 0.2,\n      sheen: 0.18,\n      roughness: 0.58,\n      artworkProtection: 0.72,\n      heroProtection: 0.78,\n      shadowOpacity: 0.48,\n      shadowSoftness: 38,\n      shadowDistance: 118,\n      contactStrength: 0.72,\n      backgroundSpill: 0.54,\n      spillFocus: 0.66,\n      goboStrength: 0.32,\n      breath: 0.16,\n      gobo: \"window\",\n    },\n  ),\n  definePreset(\n    \"projector-haze\",\n    \"Projector Haze\",\n    \"Frontal pool \u00b7 optical memory\",\n    \"A contained frontal pool with soft falloff. The world feels projected rather than gradient-filled, while the slide stays legible.\",\n    \"Archive, evidence, screenings, documentary history\",\n    {\n      enabled: true,\n      space: \"stage\",\n      motionMode: \"flicker\",\n      motionSpeed: 2,\n      keyColor: \"#ffe2b2\",\n      fillColor: \"#566789\",\n      shadowColor: \"#09090f\",\n      azimuth: 94,\n      elevation: 68,\n      keyIntensity: 0.86,\n      fillIntensity: 0.38,\n      rimIntensity: 0.08,\n      sheen: 0.1,\n      roughness: 0.78,\n      artworkProtection: 0.78,\n      heroProtection: 0.88,\n      shadowOpacity: 0.32,\n      shadowSoftness: 72,\n      shadowDistance: 42,\n      contactStrength: 0.56,\n      backgroundSpill: 0.7,\n      spillFocus: 0.5,\n      goboStrength: 0.24,\n      breath: 0.22,\n      gobo: \"projector\",\n    },\n  ),\n  definePreset(\n    \"noir-slice\",\n    \"Noir Slice\",\n    \"Hard cut \u00b7 deep negative fill\",\n    \"A low hard source, near-black fill, and narrow atmospheric slash. It creates dread without swallowing the hero slide.\",\n    \"Horror, thriller, noir, psychological tension\",\n    {\n      enabled: true,\n      space: \"stage\",\n      motionMode: \"static\",\n      motionSpeed: 1,\n      keyColor: \"#edf4ff\",\n      fillColor: \"#161a2a\",\n      shadowColor: \"#000000\",\n      azimuth: 164,\n      elevation: 18,\n      keyIntensity: 1.34,\n      fillIntensity: 0.08,\n      rimIntensity: 0.07,\n      sheen: 0.28,\n      roughness: 0.4,\n      artworkProtection: 0.54,\n      heroProtection: 0.82,\n      shadowOpacity: 0.72,\n      shadowSoftness: 18,\n      shadowDistance: 164,\n      contactStrength: 0.9,\n      backgroundSpill: 0.64,\n      spillFocus: 0.3,\n      goboStrength: 0.46,\n      breath: 0.04,\n      gobo: \"slit\",\n    },\n  ),\n  definePreset(\n    \"golden-hour\",\n    \"Golden Hour\",\n    \"Low amber key \u00b7 violet air\",\n    \"A low warm source with restrained violet fill, generous penumbra, and a slowly travelling horizon glow.\",\n    \"Romance, tenderness, nostalgia, family stories\",\n    {\n      enabled: true,\n      space: \"stage\",\n      motionMode: \"sweep\",\n      motionSpeed: 1,\n      keyColor: \"#ffbd66\",\n      fillColor: \"#777fba\",\n      shadowColor: \"#31170f\",\n      azimuth: 24,\n      elevation: 16,\n      keyIntensity: 1.08,\n      fillIntensity: 0.31,\n      rimIntensity: 0.26,\n      sheen: 0.2,\n      roughness: 0.54,\n      artworkProtection: 0.68,\n      heroProtection: 0.82,\n      shadowOpacity: 0.5,\n      shadowSoftness: 58,\n      shadowDistance: 172,\n      contactStrength: 0.66,\n      backgroundSpill: 0.58,\n      spillFocus: 0.9,\n      goboStrength: 0.24,\n      breath: 0.18,\n      gobo: \"sunset\",\n    },\n  ),\n  definePreset(\n    \"electric-rim\",\n    \"Electric Rim\",\n    \"Cyan edge \u00b7 ultraviolet fill\",\n    \"A cyan edge and ultraviolet fill for nocturnal worlds, with bounded gloss so typography remains the subject.\",\n    \"Music, speculative fiction, nightlife, technology\",\n    {\n      enabled: true,\n      space: \"card\",\n      motionMode: \"orbit\",\n      motionSpeed: 2,\n      keyColor: \"#8feeff\",\n      fillColor: \"#d081ff\",\n      shadowColor: \"#070311\",\n      azimuth: -28,\n      elevation: 42,\n      keyIntensity: 0.84,\n      fillIntensity: 0.35,\n      rimIntensity: 0.74,\n      sheen: 0.36,\n      roughness: 0.34,\n      artworkProtection: 0.62,\n      heroProtection: 0.76,\n      shadowOpacity: 0.44,\n      shadowSoftness: 46,\n      shadowDistance: 92,\n      contactStrength: 0.5,\n      backgroundSpill: 0.5,\n      spillFocus: 0.58,\n      goboStrength: 0.28,\n      breath: 0.32,\n      gobo: \"edge\",\n    },\n  ),\n  definePreset(\n    \"overcast-window\",\n    \"Overcast Window\",\n    \"Cloud-soft daylight \u00b7 almost shadowless\",\n    \"Broad cool daylight and barely-there cast shadow. It gives quiet dimension while protecting every authored colour decision.\",\n    \"Drama, documentary, architecture, restrained editorial\",\n    {\n      enabled: true,\n      space: \"stage\",\n      motionMode: \"breathe\",\n      motionSpeed: 1,\n      keyColor: \"#e8f0ff\",\n      fillColor: \"#d8d2c5\",\n      shadowColor: \"#151820\",\n      azimuth: 118,\n      elevation: 64,\n      keyIntensity: 0.62,\n      fillIntensity: 0.72,\n      rimIntensity: 0.05,\n      sheen: 0.05,\n      roughness: 0.9,\n      artworkProtection: 0.94,\n      heroProtection: 0.94,\n      shadowOpacity: 0.18,\n      shadowSoftness: 112,\n      shadowDistance: 30,\n      contactStrength: 0.34,\n      backgroundSpill: 0.38,\n      spillFocus: 1.18,\n      goboStrength: 0.08,\n      breath: 0.06,\n      gobo: \"overcast\",\n    },\n  ),\n  definePreset(\n    \"moon-pool\",\n    \"Moon Pool\",\n    \"Cold circle \u00b7 deep blue air\",\n    \"A high cool pool with a slow orbital drift, soft enough for dream logic and severe enough for night.\",\n    \"Fantasy, dream, night exteriors, solitude\",\n    {\n      enabled: true,\n      space: \"stage\",\n      motionMode: \"orbit\",\n      motionSpeed: 1,\n      keyColor: \"#cadcff\",\n      fillColor: \"#24345e\",\n      shadowColor: \"#02040d\",\n      azimuth: -104,\n      elevation: 74,\n      keyIntensity: 0.88,\n      fillIntensity: 0.18,\n      rimIntensity: 0.34,\n      sheen: 0.22,\n      roughness: 0.6,\n      artworkProtection: 0.7,\n      heroProtection: 0.86,\n      shadowOpacity: 0.46,\n      shadowSoftness: 82,\n      shadowDistance: 44,\n      contactStrength: 0.52,\n      backgroundSpill: 0.74,\n      spillFocus: 0.46,\n      goboStrength: 0.3,\n      breath: 0.2,\n      gobo: \"moon\",\n    },\n  ),\n  definePreset(\n    \"sodium-vapor\",\n    \"Sodium Vapor\",\n    \"Street amber \u00b7 hard urban cast\",\n    \"A narrow amber street source with dirty green fill and an assertive cast. Industrial, lonely, and materially specific.\",\n    \"Crime, urban night, road films, industrial work\",\n    {\n      enabled: true,\n      space: \"stage\",\n      motionMode: \"flicker\",\n      motionSpeed: 2,\n      keyColor: \"#ffac38\",\n      fillColor: \"#6d7d62\",\n      shadowColor: \"#170b02\",\n      azimuth: 152,\n      elevation: 34,\n      keyIntensity: 1.18,\n      fillIntensity: 0.18,\n      rimIntensity: 0.08,\n      sheen: 0.2,\n      roughness: 0.52,\n      artworkProtection: 0.58,\n      heroProtection: 0.74,\n      shadowOpacity: 0.58,\n      shadowSoftness: 28,\n      shadowDistance: 126,\n      contactStrength: 0.78,\n      backgroundSpill: 0.66,\n      spillFocus: 0.42,\n      goboStrength: 0.42,\n      breath: 0.22,\n      gobo: \"sodium\",\n    },\n  ),\n  definePreset(\n    \"lantern-flicker\",\n    \"Lantern Flicker\",\n    \"Warm local source \u00b7 imperfect pulse\",\n    \"A small warm source bound to each card, with deterministic asymmetric flicker and a soft intimate falloff.\",\n    \"Folklore, ritual, historical drama, intimate horror\",\n    {\n      enabled: true,\n      space: \"card\",\n      motionMode: \"flicker\",\n      motionSpeed: 3,\n      keyColor: \"#ffb05a\",\n      fillColor: \"#6b3441\",\n      shadowColor: \"#1a0805\",\n      azimuth: 58,\n      elevation: 38,\n      keyIntensity: 1.22,\n      fillIntensity: 0.24,\n      rimIntensity: 0.16,\n      sheen: 0.12,\n      roughness: 0.68,\n      artworkProtection: 0.66,\n      heroProtection: 0.8,\n      shadowOpacity: 0.52,\n      shadowSoftness: 48,\n      shadowDistance: 86,\n      contactStrength: 0.7,\n      backgroundSpill: 0.46,\n      spillFocus: 0.38,\n      goboStrength: 0.34,\n      breath: 0.34,\n      gobo: \"lantern\",\n    },\n  ),\n  definePreset(\n    \"fluorescent-flat\",\n    \"Fluorescent Flat\",\n    \"Ceiling strip \u00b7 institutional unease\",\n    \"Cool overhead light, broad shallow shadow, and a nearly static ceiling field. Clinical rather than glossy.\",\n    \"Workplace, hospital, bureaucracy, procedural stories\",\n    {\n      enabled: true,\n      space: \"stage\",\n      motionMode: \"flicker\",\n      motionSpeed: 4,\n      keyColor: \"#d8ffe8\",\n      fillColor: \"#b4c7d8\",\n      shadowColor: \"#11181a\",\n      azimuth: 90,\n      elevation: 82,\n      keyIntensity: 0.74,\n      fillIntensity: 0.68,\n      rimIntensity: 0.02,\n      sheen: 0.04,\n      roughness: 0.92,\n      artworkProtection: 0.9,\n      heroProtection: 0.92,\n      shadowOpacity: 0.24,\n      shadowSoftness: 68,\n      shadowDistance: 22,\n      contactStrength: 0.46,\n      backgroundSpill: 0.5,\n      spillFocus: 1.12,\n      goboStrength: 0.16,\n      breath: 0.12,\n      gobo: \"ceiling\",\n    },\n  ),\n  definePreset(\n    \"headlight-sweep\",\n    \"Headlight Sweep\",\n    \"Twin beams \u00b7 travelling urgency\",\n    \"Two hard travelling beams rake across the stage with a long directional cast. Built for momentum rather than ambience.\",\n    \"Thriller, chase, road night, kinetic suspense\",\n    {\n      enabled: true,\n      space: \"stage\",\n      motionMode: \"sweep\",\n      motionSpeed: 3,\n      keyColor: \"#fff7df\",\n      fillColor: \"#48627f\",\n      shadowColor: \"#020409\",\n      azimuth: -8,\n      elevation: 12,\n      keyIntensity: 1.38,\n      fillIntensity: 0.14,\n      rimIntensity: 0.22,\n      sheen: 0.26,\n      roughness: 0.46,\n      artworkProtection: 0.52,\n      heroProtection: 0.8,\n      shadowOpacity: 0.68,\n      shadowSoftness: 24,\n      shadowDistance: 178,\n      contactStrength: 0.84,\n      backgroundSpill: 0.8,\n      spillFocus: 0.32,\n      goboStrength: 0.54,\n      breath: 0.42,\n      gobo: \"headlights\",\n    },\n  ),\n];\n\nconst COMPARED_LIGHTING_KEYS: readonly (keyof LightingSettings)[] = [\n  \"version\",\n  \"space\",\n  \"motionMode\",\n  \"motionSpeed\",\n  \"keyColor\",\n  \"fillColor\",\n  \"shadowColor\",\n  \"azimuth\",\n  \"elevation\",\n  \"keyIntensity\",\n  \"fillIntensity\",\n  \"rimIntensity\",\n  \"sheen\",\n  \"roughness\",\n  \"artworkProtection\",\n  \"heroProtection\",\n  \"shadowOpacity\",\n  \"shadowSoftness\",\n  \"shadowDistance\",\n  \"contactStrength\",\n  \"backgroundSpill\",\n  \"spillFocus\",\n  \"goboStrength\",\n  \"breath\",\n  \"gobo\",\n];\n\nexport function getLightingPreset(id: LightingPresetId): LightingPreset {\n  return LIGHTING_PRESETS.find((preset) => preset.id === id) ?? LIGHTING_PRESETS[0]!;\n}\n\nexport function applyLightingPreset(\n  current: LightingSettings,\n  id: LightingPresetId,\n): LightingSettings {\n  if (id === \"custom\") return { ...current };\n  const enabled = current.enabled;\n  return { ...getLightingPreset(id).lighting, enabled };\n}\n\nexport function isLightingCustomized(settings: LightingSettings): boolean {\n  if (settings.preset === \"custom\") return true;\n  const authored = getLightingPreset(settings.preset).lighting;\n  return COMPARED_LIGHTING_KEYS.some((key) => settings[key] !== authored[key]);\n}\n\nexport interface LightingTimeline {\n  time: number;\n  reduced: boolean;\n  seamless: boolean;\n  duration: number;\n  loops: number;\n}\n\nexport interface ResolvedLightingFrame {\n  direction: [number, number, number];\n  screenDirection: [number, number];\n  shadowOffset: [number, number];\n  fieldCenter: [number, number];\n  phase: number;\n  intensity: number;\n  goboMode: number;\n}\n\nconst TAU = Math.PI * 2;\nconst GOBO_MODES: Record<LightGobo, number> = {\n  softbox: 0,\n  window: 1,\n  projector: 2,\n  slit: 3,\n  sunset: 4,\n  edge: 5,\n  overcast: 6,\n  moon: 7,\n  sodium: 8,\n  lantern: 9,\n  ceiling: 10,\n  headlights: 11,\n};\n\nconst MOTION_HARMONICS: Record<LightingMotion, readonly [number, number, number]> = {\n  static: [0, 0, 0],\n  breathe: [1, 2, 1],\n  sweep: [1, 2, 1],\n  flicker: [3, 5, 7],\n  orbit: [1, 2, 3],\n};\n\nfunction normalise2(x: number, y: number): [number, number] {\n  const length = Math.hypot(x, y) || 1;\n  return [x / length, y / length];\n}\n\nfunction normalise3(x: number, y: number, z: number): [number, number, number] {\n  const length = Math.hypot(x, y, z) || 1;\n  return [x / length, y / length, z / length];\n}\n\nfunction clamp(value: number, min: number, max: number): number {\n  return Math.min(max, Math.max(min, value));\n}\n\nexport function lightingNeedsContinuousFrames(settings: LightingSettings, reduced: boolean): boolean {\n  return settings.enabled\n    && !reduced\n    && settings.motionMode !== \"static\"\n    && settings.breath > 0;\n}\n\nfunction resolvePhase(settings: LightingSettings, timeline: LightingTimeline): number {\n  if (!lightingNeedsContinuousFrames(settings, timeline.reduced)) return 0;\n  const speed = clamp(Math.round(settings.motionSpeed), 1, 4);\n  if (timeline.seamless) {\n    const duration = Math.max(0.001, timeline.duration);\n    const loops = Math.max(1, Math.round(timeline.loops));\n    return (timeline.time / duration) * TAU * loops * speed;\n  }\n  return timeline.time * 0.22 * speed;\n}\n\ninterface MotionState {\n  azimuthOffset: number;\n  elevationOffset: number;\n  fieldX: number;\n  fieldY: number;\n  distanceScale: number;\n  intensity: number;\n}\n\nfunction resolveMotion(\n  mode: LightingMotion,\n  phase: number,\n  amount: number,\n): MotionState {\n  const safeAmount = clamp(amount, 0, 1);\n  const harmonics = MOTION_HARMONICS[mode];\n  if (mode === \"static\" || safeAmount === 0 || phase === 0) {\n    return {\n      azimuthOffset: 0,\n      elevationOffset: 0,\n      fieldX: 0,\n      fieldY: 0,\n      distanceScale: 1,\n      intensity: 1,\n    };\n  }\n\n  const a = Math.sin(phase * harmonics[0]);\n  const b = Math.sin(phase * harmonics[1] + 1.1);\n  const c = Math.cos(phase * harmonics[2] - 0.7);\n\n  switch (mode) {\n    case \"breathe\":\n      return {\n        azimuthOffset: a * safeAmount * 2.4,\n        elevationOffset: b * safeAmount * 0.8,\n        fieldX: a * safeAmount * 0.035,\n        fieldY: b * safeAmount * 0.024,\n        distanceScale: 1 + a * safeAmount * 0.025,\n        intensity: 1 + b * safeAmount * 0.025,\n      };\n    case \"sweep\":\n      return {\n        azimuthOffset: a * safeAmount * 13,\n        elevationOffset: b * safeAmount * 1.8,\n        fieldX: a * safeAmount * 0.28,\n        fieldY: b * safeAmount * 0.055,\n        distanceScale: 1 + c * safeAmount * 0.035,\n        intensity: 1 + b * safeAmount * 0.045,\n      };\n    case \"flicker\": {\n      // Integer harmonics give irregular-feeling but exactly closing movement.\n      const flicker = a * 0.54 + b * 0.31 + c * 0.15;\n      return {\n        azimuthOffset: b * safeAmount * 1.7,\n        elevationOffset: c * safeAmount * 1.1,\n        fieldX: b * safeAmount * 0.028,\n        fieldY: c * safeAmount * 0.022,\n        distanceScale: 1 + flicker * safeAmount * 0.025,\n        intensity: 1 + flicker * safeAmount * 0.16,\n      };\n    }\n    case \"orbit\":\n      return {\n        azimuthOffset: a * safeAmount * 8,\n        elevationOffset: b * safeAmount * 2.4,\n        fieldX: Math.cos(phase) * safeAmount * 0.16,\n        fieldY: Math.sin(phase) * safeAmount * 0.12,\n        distanceScale: 1 + c * safeAmount * 0.025,\n        intensity: 1 + b * safeAmount * 0.035,\n      };\n    case \"static\":\n    default:\n      return {\n        azimuthOffset: 0,\n        elevationOffset: 0,\n        fieldX: 0,\n        fieldY: 0,\n        distanceScale: 1,\n        intensity: 1,\n      };\n  }\n}\n\n/**\n * Resolves authored controls into a deterministic frame. Preview and export\n * share this compiler. Every animated mode uses integer harmonics: seamless\n * masters close completely, reduced motion freezes the whole state, and no\n * quantity depends on wall-clock randomness.\n */\nexport function resolveLightingFrame(\n  settings: LightingSettings,\n  timeline: LightingTimeline,\n): ResolvedLightingFrame {\n  const phase = resolvePhase(settings, timeline);\n  const motion = resolveMotion(settings.motionMode, phase, settings.breath);\n  const azimuth = (settings.azimuth + motion.azimuthOffset) * Math.PI / 180;\n  const elevationDegrees = clamp(settings.elevation + motion.elevationOffset, 5, 85);\n  const elevation = elevationDegrees * Math.PI / 180;\n  const planar = Math.cos(elevation);\n  const direction = normalise3(\n    Math.cos(azimuth) * planar,\n    Math.sin(azimuth) * planar,\n    Math.max(0.06, Math.sin(elevation)),\n  );\n  const screenDirection = normalise2(direction[0], direction[1]);\n  const castDirection = normalise2(-screenDirection[0], -screenDirection[1]);\n  const elevationScale = 0.34 + Math.cos(elevation) * 0.66;\n  const distance = Math.min(\n    settings.shadowDistance,\n    settings.shadowDistance * elevationScale * motion.distanceScale,\n  );\n\n  return {\n    direction,\n    screenDirection,\n    shadowOffset: [castDirection[0] * distance, castDirection[1] * distance],\n    fieldCenter: [\n      clamp(screenDirection[0] * 0.18 + motion.fieldX, -0.45, 0.45),\n      clamp(screenDirection[1] * 0.18 + motion.fieldY, -0.45, 0.45),\n    ],\n    phase,\n    intensity: clamp(motion.intensity, 0.82, 1.18),\n    goboMode: GOBO_MODES[settings.gobo],\n  };\n}\n")
write("tests/lighting.test.ts", "import { describe, expect, it } from \"vitest\";\nimport {\n  applyLightingPreset,\n  getLightingPreset,\n  isLightingCustomized,\n  LIGHTING_PRESETS,\n  lightingNeedsContinuousFrames,\n  resolveLightingFrame,\n} from \"../src/lighting\";\nimport { DEFAULT_SETTINGS, cloneSettings, type LightingSettings } from \"../src/model\";\nimport { THEMES } from \"../src/themes\";\nimport { validateStudioSettings } from \"../src/lib/settingsValidation\";\n\nfunction frame(\n  lighting: LightingSettings,\n  time: number,\n  overrides: Partial<Parameters<typeof resolveLightingFrame>[1]> = {},\n) {\n  return resolveLightingFrame(lighting, {\n    time,\n    reduced: false,\n    seamless: false,\n    duration: 8,\n    loops: 1,\n    ...overrides,\n  });\n}\n\ndescribe(\"authored lighting direction\", () => {\n  it(\"ships twelve structurally distinct rigs rather than palette swaps\", () => {\n    expect(LIGHTING_PRESETS).toHaveLength(12);\n    expect(new Set(LIGHTING_PRESETS.map((preset) => preset.id)).size).toBe(12);\n    expect(new Set(LIGHTING_PRESETS.map((preset) => preset.lighting.gobo)).size).toBe(12);\n\n    const signatures = LIGHTING_PRESETS.map(({ lighting }) => JSON.stringify({\n      space: lighting.space,\n      motion: [lighting.motionMode, lighting.motionSpeed, lighting.breath],\n      source: [lighting.azimuth, lighting.elevation, lighting.keyIntensity, lighting.fillIntensity],\n      surface: [lighting.rimIntensity, lighting.sheen, lighting.roughness, lighting.artworkProtection],\n      shadow: [\n        lighting.shadowOpacity,\n        lighting.shadowSoftness,\n        lighting.shadowDistance,\n        lighting.contactStrength,\n      ],\n      field: [lighting.backgroundSpill, lighting.spillFocus, lighting.goboStrength, lighting.gobo],\n    }));\n    expect(new Set(signatures).size).toBe(LIGHTING_PRESETS.length);\n\n    for (const preset of LIGHTING_PRESETS) {\n      const settings = cloneSettings(DEFAULT_SETTINGS);\n      settings.lighting = { ...preset.lighting };\n      expect(validateStudioSettings(settings).lighting).toEqual(preset.lighting);\n    }\n  });\n\n  it(\"keeps every film world wired to an authored lighting rig\", () => {\n    for (const theme of THEMES) {\n      expect(theme.settings.lighting.preset).not.toBe(\"custom\");\n      expect(getLightingPreset(theme.settings.lighting.preset).id)\n        .toBe(theme.settings.lighting.preset);\n    }\n  });\n\n  it(\"applies a rig without mutating source state or overriding an intentional bypass\", () => {\n    const current = cloneSettings(DEFAULT_SETTINGS).lighting;\n    current.enabled = false;\n    const next = applyLightingPreset(current, \"moon-pool\");\n\n    expect(next).toEqual({\n      ...getLightingPreset(\"moon-pool\").lighting,\n      enabled: false,\n    });\n    expect(next).not.toBe(current);\n    expect(current.preset).toBe(\"studio-soft\");\n  });\n\n  it(\"tracks tuned rigs without treating the master bypass as customization\", () => {\n    const authored = { ...getLightingPreset(\"studio-soft\").lighting };\n    expect(isLightingCustomized(authored)).toBe(false);\n\n    authored.enabled = false;\n    expect(isLightingCustomized(authored)).toBe(false);\n\n    authored.shadowSoftness += 1;\n    expect(isLightingCustomized(authored)).toBe(true);\n\n    const reset = applyLightingPreset(authored, authored.preset);\n    expect(isLightingCustomized(reset)).toBe(false);\n    expect(reset.enabled).toBe(false);\n  });\n\n  it(\"resolves finite normalized directions and bounded fields across every rig\", () => {\n    for (const preset of LIGHTING_PRESETS) {\n      for (const time of [0, 0.125, 3.75, 8, 93.4]) {\n        const resolved = frame(preset.lighting, time);\n        expect(resolved.direction.every(Number.isFinite)).toBe(true);\n        expect(resolved.screenDirection.every(Number.isFinite)).toBe(true);\n        expect(resolved.shadowOffset.every(Number.isFinite)).toBe(true);\n        expect(resolved.fieldCenter.every(Number.isFinite)).toBe(true);\n        expect(Math.hypot(...resolved.direction)).toBeCloseTo(1, 8);\n        expect(Math.hypot(...resolved.screenDirection)).toBeCloseTo(1, 8);\n        expect(Math.hypot(...resolved.shadowOffset))\n          .toBeLessThanOrEqual(preset.lighting.shadowDistance + 1e-8);\n        expect(Math.abs(resolved.fieldCenter[0])).toBeLessThanOrEqual(0.45);\n        expect(Math.abs(resolved.fieldCenter[1])).toBeLessThanOrEqual(0.45);\n        expect(resolved.intensity).toBeGreaterThanOrEqual(0.82);\n        expect(resolved.intensity).toBeLessThanOrEqual(1.18);\n        expect(resolved.goboMode).toBeGreaterThanOrEqual(0);\n        expect(resolved.goboMode).toBeLessThan(12);\n      }\n    }\n  });\n\n  it(\"closes every animated rig exactly under seamless export\", () => {\n    for (const preset of LIGHTING_PRESETS) {\n      const timeline = {\n        reduced: false,\n        seamless: true,\n        duration: 9,\n        loops: 3,\n      };\n      const start = resolveLightingFrame(preset.lighting, { ...timeline, time: 0 });\n      const end = resolveLightingFrame(preset.lighting, { ...timeline, time: 9 });\n\n      for (const key of [\"direction\", \"screenDirection\", \"shadowOffset\", \"fieldCenter\"] as const) {\n        expect(end[key][0]).toBeCloseTo(start[key][0], 10);\n        expect(end[key][1]).toBeCloseTo(start[key][1], 10);\n      }\n      expect(end.direction[2]).toBeCloseTo(start.direction[2], 10);\n      expect(end.intensity).toBeCloseTo(start.intensity, 10);\n      expect(end.goboMode).toBe(start.goboMode);\n    }\n  });\n\n  it(\"freezes the complete state for reduced motion and static light\", () => {\n    const moving = { ...getLightingPreset(\"headlight-sweep\").lighting };\n    const reducedStart = frame(moving, 0, { reduced: true });\n    const reducedLater = frame(moving, 1_000_000, { reduced: true });\n    expect(reducedLater).toEqual(reducedStart);\n\n    const staticRig = { ...moving, motionMode: \"static\" as const, breath: 1 };\n    expect(frame(staticRig, 0)).toEqual(frame(staticRig, 1_000_000));\n  });\n\n  it(\"requests continuous preview frames only when light can actually move\", () => {\n    const lighting = { ...DEFAULT_SETTINGS.lighting };\n    expect(lightingNeedsContinuousFrames(lighting, false)).toBe(true);\n    expect(lightingNeedsContinuousFrames(lighting, true)).toBe(false);\n    expect(lightingNeedsContinuousFrames({ ...lighting, enabled: false }, false)).toBe(false);\n    expect(lightingNeedsContinuousFrames({ ...lighting, breath: 0 }, false)).toBe(false);\n    expect(lightingNeedsContinuousFrames({ ...lighting, motionMode: \"static\" }, false)).toBe(false);\n  });\n\n  it(\"shortens the cast as the source rises without exceeding authored reach\", () => {\n    const base = { ...DEFAULT_SETTINGS.lighting, shadowDistance: 180, motionMode: \"static\" as const };\n    const low = frame({ ...base, elevation: 5 }, 0);\n    const high = frame({ ...base, elevation: 85 }, 0);\n\n    expect(Math.hypot(...low.shadowOffset)).toBeGreaterThan(Math.hypot(...high.shadowOffset));\n    expect(Math.hypot(...low.shadowOffset)).toBeLessThanOrEqual(180);\n  });\n\n  it(\"keeps all four motion speeds deterministic and exactly closing\", () => {\n    const base = { ...getLightingPreset(\"lantern-flicker\").lighting };\n    for (const motionSpeed of [1, 2, 3, 4] as const) {\n      const lighting = { ...base, motionSpeed };\n      const start = frame(lighting, 0, { seamless: true, duration: 11, loops: 2 });\n      const end = frame(lighting, 11, { seamless: true, duration: 11, loops: 2 });\n      expect(end.direction[0]).toBeCloseTo(start.direction[0], 10);\n      expect(end.direction[1]).toBeCloseTo(start.direction[1], 10);\n      expect(end.direction[2]).toBeCloseTo(start.direction[2], 10);\n      expect(end.intensity).toBeCloseTo(start.intensity, 10);\n    }\n  });\n});\n")
write("tests/lightingValidation.test.ts", "import { describe, expect, it } from \"vitest\";\nimport {\n  DEFAULT_SETTINGS,\n  LIGHTING_VERSION,\n  cloneSettings,\n} from \"../src/model\";\nimport {\n  SettingsValidationError,\n  validateStudioSettings,\n} from \"../src/lib/settingsValidation\";\n\nfunction candidate(): Record<string, any> {\n  return cloneSettings(DEFAULT_SETTINGS) as unknown as Record<string, any>;\n}\n\nfunction expectInvalidCandidate(source: unknown, path: string): SettingsValidationError {\n  try {\n    validateStudioSettings(source);\n  } catch (error) {\n    expect(error).toBeInstanceOf(SettingsValidationError);\n    expect(error).toMatchObject({\n      name: \"SettingsValidationError\",\n      code: \"INVALID_STUDIO_SETTINGS\",\n      path,\n    });\n    return error as SettingsValidationError;\n  }\n  throw new Error(`Expected ${path} to fail validation`);\n}\n\nfunction expectLightingInvalid(path: string, value: unknown): void {\n  const source = candidate();\n  source.lighting[path] = value;\n  expectInvalidCandidate(source, `settings.lighting.${path}`);\n}\n\ndescribe(\"lighting settings trust boundary\", () => {\n  it(\"hydrates pre-lighting schema-v1 projects from their visible legacy shadow values\", () => {\n    const source = candidate();\n    source.slide.shadowOpacity = 0.61;\n    source.slide.shadowSoftness = 73;\n    delete source.lighting;\n\n    const result = validateStudioSettings(source);\n    expect(result.lighting).toEqual({\n      ...DEFAULT_SETTINGS.lighting,\n      shadowOpacity: 0.61,\n      shadowSoftness: 73,\n    });\n  });\n\n  it(\"upgrades first-generation lighting objects with only additive v2 fields\", () => {\n    const source = candidate();\n    const firstGeneration = { ...source.lighting };\n    for (const key of [\n      \"version\",\n      \"space\",\n      \"motionMode\",\n      \"motionSpeed\",\n      \"artworkProtection\",\n      \"heroProtection\",\n      \"goboStrength\",\n    ]) delete firstGeneration[key];\n    source.lighting = firstGeneration;\n\n    const result = validateStudioSettings(source);\n    expect(result.lighting).toEqual({\n      ...firstGeneration,\n      version: LIGHTING_VERSION,\n      space: DEFAULT_SETTINGS.lighting.space,\n      motionMode: DEFAULT_SETTINGS.lighting.motionMode,\n      motionSpeed: DEFAULT_SETTINGS.lighting.motionSpeed,\n      artworkProtection: DEFAULT_SETTINGS.lighting.artworkProtection,\n      heroProtection: DEFAULT_SETTINGS.lighting.heroProtection,\n      goboStrength: DEFAULT_SETTINGS.lighting.goboStrength,\n    });\n  });\n\n  it(\"does not silently repair supplied malformed fields from either lighting generation\", () => {\n    const missing = candidate();\n    delete missing.lighting.keyIntensity;\n    expectInvalidCandidate(missing, \"settings.lighting.keyIntensity\");\n\n    const malformedVersion = candidate();\n    malformedVersion.lighting.version = LIGHTING_VERSION + 1;\n    expectInvalidCandidate(malformedVersion, \"settings.lighting.version\");\n\n    const wrongShape = candidate();\n    wrongShape.lighting = [];\n    expectInvalidCandidate(wrongShape, \"settings.lighting\");\n  });\n\n  it(\"accepts every surfaced lower and upper boundary\", () => {\n    const lower = candidate();\n    Object.assign(lower.lighting, {\n      version: LIGHTING_VERSION,\n      preset: \"custom\",\n      enabled: false,\n      space: \"stage\",\n      motionMode: \"static\",\n      motionSpeed: 1,\n      azimuth: -180,\n      elevation: 5,\n      keyIntensity: 0,\n      fillIntensity: 0,\n      rimIntensity: 0,\n      sheen: 0,\n      roughness: 0,\n      artworkProtection: 0,\n      heroProtection: 0,\n      shadowOpacity: 0,\n      shadowSoftness: 2,\n      shadowDistance: 0,\n      contactStrength: 0,\n      backgroundSpill: 0,\n      spillFocus: 0.15,\n      goboStrength: 0,\n      breath: 0,\n      gobo: \"softbox\",\n    });\n    expect(validateStudioSettings(lower)).toEqual(lower);\n\n    const upper = candidate();\n    Object.assign(upper.lighting, {\n      version: LIGHTING_VERSION,\n      preset: \"headlight-sweep\",\n      enabled: true,\n      space: \"card\",\n      motionMode: \"orbit\",\n      motionSpeed: 4,\n      azimuth: 180,\n      elevation: 85,\n      keyIntensity: 2,\n      fillIntensity: 1,\n      rimIntensity: 1,\n      sheen: 1,\n      roughness: 1,\n      artworkProtection: 1,\n      heroProtection: 1,\n      shadowOpacity: 0.9,\n      shadowSoftness: 180,\n      shadowDistance: 180,\n      contactStrength: 1,\n      backgroundSpill: 1,\n      spillFocus: 1.5,\n      goboStrength: 1,\n      breath: 1,\n      gobo: \"headlights\",\n    });\n    expect(validateStudioSettings(upper)).toEqual(upper);\n  });\n\n  it(\"rejects enum, colour, non-finite, integer, and range escapes\", () => {\n    for (const [path, value] of [\n      [\"preset\", \"palette-swap\"],\n      [\"gobo\", \"venetian\"],\n      [\"space\", \"world\"],\n      [\"motionMode\", \"random\"],\n      [\"motionSpeed\", 0],\n      [\"motionSpeed\", 2.5],\n      [\"motionSpeed\", 5],\n      [\"keyColor\", \"white\"],\n      [\"fillColor\", \"#fff\"],\n      [\"shadowColor\", \"#00000000\"],\n      [\"enabled\", 1],\n      [\"azimuth\", 181],\n      [\"elevation\", 4.999],\n      [\"keyIntensity\", 2.001],\n      [\"fillIntensity\", -0.001],\n      [\"rimIntensity\", Number.NaN],\n      [\"sheen\", Number.POSITIVE_INFINITY],\n      [\"roughness\", -0.001],\n      [\"artworkProtection\", 1.001],\n      [\"heroProtection\", -0.001],\n      [\"shadowOpacity\", 0.901],\n      [\"shadowSoftness\", 1.999],\n      [\"shadowDistance\", 180.001],\n      [\"contactStrength\", 1.001],\n      [\"backgroundSpill\", -0.001],\n      [\"spillFocus\", 1.501],\n      [\"goboStrength\", 1.001],\n      [\"breath\", 1.001],\n    ] as const) expectLightingInvalid(path, value);\n  });\n\n  it(\"drops unknown lighting keys without invoking hostile getters\", () => {\n    const source = candidate();\n    source.lighting.futureLighting = { hidden: true };\n    Object.defineProperty(source.lighting, \"hostileGetter\", {\n      enumerable: true,\n      get: () => {\n        throw new Error(\"unknown lighting fields must not be read\");\n      },\n    });\n\n    const result = validateStudioSettings(source);\n    expect(result.lighting).toEqual(DEFAULT_SETTINGS.lighting);\n    expect(result.lighting).not.toHaveProperty(\"futureLighting\");\n    expect(result.lighting).not.toHaveProperty(\"hostileGetter\");\n  });\n});\n")

replace_exact(
    "src/lib/settingsValidation.ts",
    'import {\n  DEFAULT_SETTINGS,\n  ENGINE_VERSION,\n  SCHEMA_VERSION,\n  SHADER_VERSION,\n  type StudioSettings,\n} from "../model";',
    'import {\n  DEFAULT_SETTINGS,\n  ENGINE_VERSION,\n  LIGHTING_VERSION,\n  SCHEMA_VERSION,\n  SHADER_VERSION,\n  type StudioSettings,\n} from "../model";',
    "lighting version import",
)
replace_exact(
    "src/lib/settingsValidation.ts",
    'const LIGHTING_PRESETS = ["custom", "studio-soft", "window-rake", "projector-haze", "noir-slice", "golden-hour", "electric-rim"] as const;\nconst LIGHT_GOBOS = ["softbox", "window", "projector", "slit", "sunset", "edge"] as const;',
    'const LIGHTING_PRESETS = [\n  "custom",\n  "studio-soft",\n  "window-rake",\n  "projector-haze",\n  "noir-slice",\n  "golden-hour",\n  "electric-rim",\n  "overcast-window",\n  "moon-pool",\n  "sodium-vapor",\n  "lantern-flicker",\n  "fluorescent-flat",\n  "headlight-sweep",\n] as const;\nconst LIGHT_GOBOS = [\n  "softbox",\n  "window",\n  "projector",\n  "slit",\n  "sunset",\n  "edge",\n  "overcast",\n  "moon",\n  "sodium",\n  "lantern",\n  "ceiling",\n  "headlights",\n] as const;\nconst LIGHTING_MOTIONS = ["static", "breathe", "sweep", "flicker", "orbit"] as const;\nconst LIGHTING_SPACES = ["stage", "card"] as const;\nconst LIGHTING_SPEEDS = [1, 2, 3, 4] as const;',
    "expanded lighting registries",
)
replace_exact(
    "src/lib/settingsValidation.ts",
    ' * known keys never receive a silent default. The only compatibility\n * exception is the additive v1 lighting section: projects written before\n * it existed inherit the old slide shadow values and the current neutral rig.',
    ' * known keys never receive a silent default. Lighting is the additive\n * compatibility exception: projects written before it existed inherit the\n * visible legacy shadow, while first-generation lighting objects inherit only\n * the new version-2 direction fields. Existing authored values are never\n * silently repaired.',
    "lighting compatibility contract",
)
replace_exact(
    "src/lib/settingsValidation.ts",
    '    lighting: {\n      preset: oneOf(lighting.preset, "settings.lighting.preset", LIGHTING_PRESETS),\n      enabled: boolean(lighting.enabled, "settings.lighting.enabled"),\n      keyColor: hexColour(lighting.keyColor, "settings.lighting.keyColor"),\n      fillColor: hexColour(lighting.fillColor, "settings.lighting.fillColor"),\n      shadowColor: hexColour(lighting.shadowColor, "settings.lighting.shadowColor"),\n      azimuth: number(lighting.azimuth, "settings.lighting.azimuth", { min: -180, max: 180 }),\n      elevation: number(lighting.elevation, "settings.lighting.elevation", { min: 5, max: 85 }),\n      keyIntensity: number(lighting.keyIntensity, "settings.lighting.keyIntensity", { min: 0, max: 2 }),\n      fillIntensity: number(lighting.fillIntensity, "settings.lighting.fillIntensity", { min: 0, max: 1 }),\n      rimIntensity: number(lighting.rimIntensity, "settings.lighting.rimIntensity", { min: 0, max: 1 }),\n      sheen: number(lighting.sheen, "settings.lighting.sheen", { min: 0, max: 1 }),\n      roughness: number(lighting.roughness, "settings.lighting.roughness", { min: 0, max: 1 }),\n      shadowOpacity: number(lighting.shadowOpacity, "settings.lighting.shadowOpacity", { min: 0, max: 0.9 }),\n      shadowSoftness: number(lighting.shadowSoftness, "settings.lighting.shadowSoftness", { min: 2, max: 180 }),\n      shadowDistance: number(lighting.shadowDistance, "settings.lighting.shadowDistance", { min: 0, max: 180 }),\n      contactStrength: number(lighting.contactStrength, "settings.lighting.contactStrength", { min: 0, max: 1 }),\n      backgroundSpill: number(lighting.backgroundSpill, "settings.lighting.backgroundSpill", { min: 0, max: 1 }),\n      spillFocus: number(lighting.spillFocus, "settings.lighting.spillFocus", { min: 0.15, max: 1.5 }),\n      breath: number(lighting.breath, "settings.lighting.breath", { min: 0, max: 1 }),\n      gobo: oneOf(lighting.gobo, "settings.lighting.gobo", LIGHT_GOBOS),\n    },',
    '    lighting: {\n      version: lighting.version === undefined\n        ? LIGHTING_VERSION\n        : literal(lighting.version, "settings.lighting.version", LIGHTING_VERSION),\n      preset: oneOf(lighting.preset, "settings.lighting.preset", LIGHTING_PRESETS),\n      enabled: boolean(lighting.enabled, "settings.lighting.enabled"),\n      space: lighting.space === undefined\n        ? DEFAULT_SETTINGS.lighting.space\n        : oneOf(lighting.space, "settings.lighting.space", LIGHTING_SPACES),\n      motionMode: lighting.motionMode === undefined\n        ? DEFAULT_SETTINGS.lighting.motionMode\n        : oneOf(lighting.motionMode, "settings.lighting.motionMode", LIGHTING_MOTIONS),\n      motionSpeed: lighting.motionSpeed === undefined\n        ? DEFAULT_SETTINGS.lighting.motionSpeed\n        : oneOf(lighting.motionSpeed, "settings.lighting.motionSpeed", LIGHTING_SPEEDS),\n      keyColor: hexColour(lighting.keyColor, "settings.lighting.keyColor"),\n      fillColor: hexColour(lighting.fillColor, "settings.lighting.fillColor"),\n      shadowColor: hexColour(lighting.shadowColor, "settings.lighting.shadowColor"),\n      azimuth: number(lighting.azimuth, "settings.lighting.azimuth", { min: -180, max: 180 }),\n      elevation: number(lighting.elevation, "settings.lighting.elevation", { min: 5, max: 85 }),\n      keyIntensity: number(lighting.keyIntensity, "settings.lighting.keyIntensity", { min: 0, max: 2 }),\n      fillIntensity: number(lighting.fillIntensity, "settings.lighting.fillIntensity", { min: 0, max: 1 }),\n      rimIntensity: number(lighting.rimIntensity, "settings.lighting.rimIntensity", { min: 0, max: 1 }),\n      sheen: number(lighting.sheen, "settings.lighting.sheen", { min: 0, max: 1 }),\n      roughness: number(lighting.roughness, "settings.lighting.roughness", { min: 0, max: 1 }),\n      artworkProtection: lighting.artworkProtection === undefined\n        ? DEFAULT_SETTINGS.lighting.artworkProtection\n        : number(lighting.artworkProtection, "settings.lighting.artworkProtection", { min: 0, max: 1 }),\n      heroProtection: lighting.heroProtection === undefined\n        ? DEFAULT_SETTINGS.lighting.heroProtection\n        : number(lighting.heroProtection, "settings.lighting.heroProtection", { min: 0, max: 1 }),\n      shadowOpacity: number(lighting.shadowOpacity, "settings.lighting.shadowOpacity", { min: 0, max: 0.9 }),\n      shadowSoftness: number(lighting.shadowSoftness, "settings.lighting.shadowSoftness", { min: 2, max: 180 }),\n      shadowDistance: number(lighting.shadowDistance, "settings.lighting.shadowDistance", { min: 0, max: 180 }),\n      contactStrength: number(lighting.contactStrength, "settings.lighting.contactStrength", { min: 0, max: 1 }),\n      backgroundSpill: number(lighting.backgroundSpill, "settings.lighting.backgroundSpill", { min: 0, max: 1 }),\n      spillFocus: number(lighting.spillFocus, "settings.lighting.spillFocus", { min: 0.15, max: 1.5 }),\n      goboStrength: lighting.goboStrength === undefined\n        ? DEFAULT_SETTINGS.lighting.goboStrength\n        : number(lighting.goboStrength, "settings.lighting.goboStrength", { min: 0, max: 1 }),\n      breath: number(lighting.breath, "settings.lighting.breath", { min: 0, max: 1 }),\n      gobo: oneOf(lighting.gobo, "settings.lighting.gobo", LIGHT_GOBOS),\n    },',
    "versioned lighting validation",
)
